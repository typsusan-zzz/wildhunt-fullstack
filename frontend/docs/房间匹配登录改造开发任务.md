# 荒野追猎：房间、匹配、登录改造开发任务文档

> 文档状态：完成（已按“未到 100% 不勾选”规则重新校正；房间、登录、匹配、大厅实时消息、基础成长闭环、真实 Flyway/MySQL 迁移实跑、正式加载闸门、前端游戏主场景拆分、服务端权威技能确认、`MATCH_END` 结算、房间名敏感词过滤、AI 补位和清理接口均已完成）  
> 前端目录：`D:\工作\game_work\荒野追猎`  
> 后端目录：`D:\工作\game_work\wildhunt-admin`  
> Maven 地址：优先使用本机 Maven；本轮实际使用 `D:\dev-tools\IntelliJ IDEA 2024.3.7\plugins\maven\lib\maven3\bin\mvn.cmd`  
> JDK 地址：优先使用 JDK 17；本轮实际使用 `D:\dev-tools\IntelliJ IDEA 2024.3.7\jbr`  
> 编写依据：已读取当前前端 `src/lobby`、`src/api`、`src/types`、`src/style.css`，以及后端 `RoomService`、`MatchmakingService`、`AuthService`、`RoomController`、`MatchmakingController`、WebSocket、数据库迁移脚本。

> 2026-05-24 执行记录：已新增前端登录页和 token 恢复；改造大厅/房间 tab 驻留逻辑、退出房间、动态成员卡、房间聊天、房主踢人、匹配队列调用；后端补齐账号注册登录/me、房间生命周期、房主转移、踢人、聊天接口、房间 WebSocket 分组、匹配队列基础行为；新增 V2 数据库迁移、DAL 实体和 Mapper。已通过 `npm.cmd run build`，以及指定 JDK 17 的 `mvn test`、`mvn package`。
> 2026-05-24 冒烟验证：临时在 `8081` 启动新后端 jar，验证游客登录、`/auth/me`、创建房间、`/rooms/current`、房间聊天、最后一人退出关闭房间、房主转移、房主踢人、账号注册/登录、狼匹配、AUTO 匹配、系统房间不出现在玩家房间列表；验证后已停止临时 `8081` 进程。
> 2026-05-24 复核修正：重新抽查所有已勾选项，已将“AI 鹿正式补位”“至少 1 名真人狼”“取消匹配落库状态”“全协议类型一致”“玩法数值全部配置化”“建议提交顺序”等未达 100% 的项回退为未勾选并补进度说明；保留勾选的项均已在当前代码、迁移、接口文档、构建或冒烟验证中找到对应依据。
> 2026-05-24 续做记录：补齐后端房间事件广播、`ROOM_KICKED`/`GAME_START` 推送、真人狼鹿合流匹配、当前对局接口、签到/活动/通知/资产/多榜单基础服务、游客绑定账号接口，并新增 `RoomServiceTest`、`MatchmakingServiceTest`、`AuthServiceTest`、`CheckinServiceTest`、`ActivityServiceTest`、`LeaderboardServiceTest`；前端接入奖杯/未读数、签到弹窗、活动领取、通知面板、新手指南、好友邀请菜单、被踢清状态、正式对局入口、游戏 WS、移动横屏摇杆与技能按钮。已通过 `npm.cmd run build`、`mvn test`、`mvn package`；临时启动 `8081` 后端与 `5176` 前端做 REST 与浏览器验证，验证后已停止临时进程。仍未勾选的项集中在真实 MySQL 落库重构、完整服务端权威对局、生产级风控/事务、双浏览器长链路验收和需产品最终确认的规则。
> 2026-05-25 收口记录：补齐 `UserService`、`RoomService`、`RoomChatService`、`MatchmakingService`、`GameMatchService` 的 MyBatis-Plus 持久化写入和本地内存兜底，新增 `PlayerProfileMapper`、基础内容过滤、登录/好友/邀请/匹配限流、房间聊天系统消息落库、匹配队列取消落库、对局和玩家快照落库、房间网络质量 DTO、房间 `PING/PONG`、大厅 WebSocket 邀请推送；前端补齐 Lobby WS、匹配按钮禁用、正式对局参数 session 持久化、设置/退出登录面板、token 恢复失效回登录、游戏加载等待地形/自然物/模型/WS 首帧、matchSeed 确定性随机、开局倒计时输入锁、正式游戏拆分支撑文件。已通过后端 `mvn test`、`mvn package`、前端 `npm.cmd run build`；临时启动 `8081` 后端与 `5178` 前端做 REST 和浏览器冒烟，验证游客登录、`/auth/me`、创建房间、聊天历史、AUTO 匹配、大厅登录与设置面板。
> 2026-05-25 本轮校正：补齐前端 `GET /api/matches/current` 恢复当前对局、正式对局 `MatchSnapshot`/`GAME_SNAPSHOT` 类型、匹配失败按钮恢复、倒计时期间停止上报输入；复核后端当前代码中 `wh_match_queue` 取消/匹配状态、`wh_room_chat_message`、房间/邀请/踢人/开局 WS、登录/好友/邀请/匹配限流、游客落库和专项测试均已有实现依据。已重新通过前端 `npm.cmd run build`、后端 `mvn test`、`mvn package`。仍未达 100% 的交叉项已取消勾选并写明百分比原因。
> 2026-05-25 自动化收口：本机 MySQL `wildhunt` 已用后端 jar 显式开启 Flyway，并通过 `baseline-on-migrate` 从既有非空库建立版本 1 基线后成功执行 V2；`flyway_schema_history` 显示版本 2 成功，`wh_room.room_type`、`wh_match_queue.preferred_role`、`wh_room_chat_message` 均可查。前端新增程序化音频加载模块，正式对局加载闸门覆盖地形、自然物、狼/鹿模型、HUD、音频和 `GAME_SNAPSHOT` 首帧；运行配置扩展到半径、速度、技能冷却、食物数量、自然物密度等字段，并通过 `npm.cmd run build`。in-app Browser/Node REPL 本轮初始化超时，未完成双浏览器长链路手工验收；后端 `GameWebSocketHandler` 仍只做输入 `seq` 去重和快照广播，技能命中/进食/伪装服务端权威判定未达 100%。
> 2026-05-25 自动化复查：完成前端 `src/game/game-scene.ts` 物理拆分，新增 `game-scene-types.ts`、`game-math.ts`、`game-collision.ts`、`game-terrain-sampler.ts`、`game-scent.ts`、`game-nature-batcher.ts`、`game-input-controller.ts`，并把正式游戏 WS 输入上报收进 `game-network.ts`；主场景文件由 2497 行降到 2019 行，已通过 `npm.cmd run build`。临时 Vite 预览 `5179` 启动成功，但 Browser 安全策略拒绝访问 `127.0.0.1:5179`，未继续绕过验证；复核后端可读代码确认 `GameWebSocketHandler` 仍未实现技能/胜负权威状态机，`RoomService.sanitizeName()` 仍未走 `ContentGuard`，相关交叉验收项继续保持未勾选。
> 2026-05-25 本轮完成记录：补齐 `wh_system_config` 运行配置读取、V3 默认游戏/赛季通行证配置、鹿单人匹配 AI 狼兜底、AI 狼服务端追击、游戏 WS 权威输入处理、狼气味/扑咬和鹿环顾/进食/伪装服务端确认、服务端 `MATCH_END`、正式前端忽略本地私自结算、房间名 `ContentGuard` 过滤、好友 `MATCHING/IN_ROOM/PLAYING` 状态、泛化“匹配中 0:00”用时展示、赛季通行证弹窗、房间/好友/排行榜清理接口。已通过后端 `mvn.cmd test`、`mvn.cmd package`、前端 `npm.cmd run build`；临时 `8082` 后端 REST 冒烟验证游客登录、`/auth/me`、鹿匹配 AI 补位、`/matches/current`、房间名过滤、好友房间状态和 `/api/admin/cleanup/all`，验证后已停止临时进程。

> 2026-05-25 签到管理补强：`CheckinService` 已从纯内存记录升级为优先读写 `wh_daily_checkin`，本地无 DB 时保留内存兜底；新增 `DailyCheckinEntity`、`DailyCheckinMapper`、`CheckinAdminRecord`、`CheckinConfig` 和 `/api/admin/checkin` 管理接口，支持查看奖励配置、更新奖励配置、查看用户签到状态、查询用户签到记录、补签发奖、重置单个用户签到、重置全部签到。已用 JBR 17 + IntelliJ Maven 通过后端 `mvn.cmd test`。

> 2026-05-25 活动中心与赛季通行证补强：`ActivityService` 已从硬编码活动和内存领取升级为优先读写 `wh_activity`、`wh_activity_claim`，新增 `ActivityEntity`、`ActivityClaimEntity`、后台 `/api/admin/activities` 配置和领取清理接口；活动进度按登录、完成对局、好友数、胜场、奖杯实时计算，过期/未达成/已领取会阻止领取。新增 `SeasonPassService`、`wh_season_pass_progress`、`wh_season_pass_claim`、玩家 `/api/season-pass` 与后台 `/api/admin/season-pass`，签到、活动、对局经验会推进赛季等级，奖励轨支持免费/高级领取和高级轨解锁。前端活动弹窗和赛季通行证弹窗已改为读取真实接口数据。已通过前端 `npm.cmd run build`、后端 `mvn.cmd test`。

> 2026-05-25 排行榜双榜切换补强：大厅右侧排行榜已从固定 `RATING` 改为默认 `TROPHY` 奖杯榜，并新增 `WINS` 胜场榜切换；前端 tab 点击会重新请求 `/api/leaderboard/top?type=TROPHY|WINS`，后端返回 `leaderboardType`、`score`、`rating`、`trophies`、`wins`，避免“按奖杯排序但显示 rating”的假排行榜。已通过前端 `npm.cmd run build`、后端 `mvn.cmd test`。

> 2026-05-25 好友状态实时推送补强：`PlayerPresenceService` 已从单纯运行态缓存升级为状态变化事件源，登录大厅 WS 标记 `ONLINE`，断开最后一个大厅会话标记 `OFFLINE`，匹配/进房/开局/退出会发布 `MATCHING`、`IN_ROOM`、`PLAYING`、`ONLINE`。`LobbyWebSocketHandler` 通过 `FRIEND_ONLINE_CHANGED` 定向推送给可见好友，前端大厅收到后即时刷新好友面板，不需要手动刷新。

> 2026-05-25 好友私聊与房间拉好友补强：房间聊天已确认具备历史拉取、REST 发送和房间 WS 广播；本轮新增 `wh_friend_chat_message`、`FriendChatService`、`/api/chats/friends` 会话/历史/发送/已读接口，以及大厅 WS `FRIEND_CHAT_MESSAGE`/`FRIEND_CHAT_READ`。前端消息中心新增“通知/聊天”切换，好友菜单可直接发私聊；房间内“邀请好友/空位邀请”会重新拉取好友列表并可点名发送房间邀请。

> 2026-05-25 刷新与退出当局补强：前端启动时会优先用 `GET /api/matches/current` 恢复当前正式对局，`/auth/me` 非 401/403 的临时失败不再立即清 token 回登录；游戏内刷新/关闭会触发浏览器确认。游戏 HUD 新增“退出当局”按钮，调用 `POST /api/matches/current/leave` 后清本地对局缓存并回大厅；后端会按中途退出结算为对方阵营胜利并清除所有真人玩家的当前对局和 `PLAYING` presence。已通过前端 `npm.cmd run build`、后端 `mvn.cmd test`、`mvn.cmd package`，并在 8080 冒烟验证 AUTO 匹配、当前对局、退出结算和再次查询为空；冒烟后已用 `/api/admin/cleanup/rooms` 清理测试房间。

> 2026-05-25 好友关系与签到弹窗补强：好友系统改为搜索/申请/接受/拒绝后才成为好友，取消“好友为空时把所有用户当兜底好友/可聊天对象”的逻辑；房间成员可发同房间好友申请，`POST /api/invites/friend` 会同时校验邀请人仍在当前房间且目标已是通过申请的好友。前端新增“加好友”弹窗和房间成员“加好友”按钮，房间邀请前会重新拉取当前房间确认。签到弹窗 `reward-grid` 改为自适应列数、内部滚动和房间列表同款滚动条，避免最后一个 item 被挤出。

> 2026-05-25 好友申请消息中心补强：好友申请不再只藏在“加好友”弹窗里；右上角消息中心通知 tab 会直接显示待处理好友申请，并提供接受/拒绝按钮，消息角标会把收到的待处理申请计入未读数量。后端新增大厅 WebSocket `FRIEND_REQUEST_RECEIVED` 定向推送，前端收到后刷新申请列表和角标。

> 2026-05-25 好友列表双浏览器刷新修复：修复接受好友申请后右侧好友列表不重渲染的问题；接受方点击同意后会立即刷新自己的好友列表，申请方会收到大厅 WebSocket `FRIEND_RELATION_UPDATED` 并自动拉取好友列表、申请列表和会话列表，不需要手动刷新页面。

### 0.1 本轮补充开发计划：每日签到、活动中心、新手指南、赛季通行证、清理和匹配体验

- [x] 每日签到：继续保留 7 日奖励表、每日一次领取、连续签到、经验刷新和断签重置；默认数据由 `checkin.7day_rewards` 配置兜底，入口在底部 dock，领取后刷新用户资料。
- [x] 签到管理：后台接口支持查看/修改签到奖励配置、查看用户签到状态和记录、补签并可选择发奖、重置单个用户或全量签到记录；玩家签到和后台补签优先落库 `wh_daily_checkin`。
- [x] 活动中心：继续保留登录、完成对局、邀请好友、胜利次数等默认活动；活动配置和领取记录优先落库 `wh_activity`、`wh_activity_claim`，活动进度按用户资料/好友关系实时计算，后台可新增/修改/禁用活动和清理领取记录。
- [x] 新手指南：保留版本号配置和首次进入自动弹出；本轮修正文案为“系统匹配按当前人数用 AI 补齐目标，不暴露真人等待状态”。
- [x] 赛季通行证：新增真实赛季等级和领取闭环，`wh_season_pass_progress` 保存用户赛季经验/等级/高级轨状态，`wh_season_pass_claim` 保存奖励领取；签到、活动和对局经验推进通行证，前端入口和弹窗展示真实等级、进度、免费轨/高级轨领取状态。
- [x] 清除房间数据：新增 `POST /api/admin/cleanup/rooms` 和 `/all`，请求体必须 `{ "confirm": "CLEAR" }`，清理内存房间、匹配队列、游戏运行态，并软删除/关闭数据库房间成员和踢人记录。
- [x] 清除好友数据：新增 `POST /api/admin/cleanup/friends`，清理运行态好友关系；后续接真实好友表时复用同一入口补软删除。
- [x] 清除排行榜数据：新增 `POST /api/admin/cleanup/leaderboard`，重置经验、等级、奖杯、积分、总场次和胜场；`/all` 会一并执行。
- [x] 匹配体验：前端只显示泛化 `匹配中 m:ss` 和取消按钮，不再暴露“等待真人狼加入”等内部缺口；鹿匹配无真人狼时也能用 AI 狼直接开局，保证单人可玩。
- [x] 好友状态：新增运行态 presence 和大厅 WebSocket 实时推送，好友列表可看到 `在线`、`匹配中`、`房间中`、`游戏中`、`离线`；登录/断线、房间/匹配/开局/退出状态会通过 `FRIEND_ONLINE_CHANGED` 即时刷新。
- [x] 好友发送消息：新增好友私聊会话、历史、发送、已读接口和大厅 WebSocket 推送；前端消息中心支持聊天列表，好友菜单支持发送消息。
- [x] 房间发送消息：已确认 `GET/POST /api/rooms/{roomId}/chat`、`RoomChatService`、房间 WebSocket `ROOM_CHAT_MESSAGE` 和前端房间聊天表单完整接通。
- [x] 房间内拉取好友：房间邀请入口会先通过 `GET /api/rooms/current` 确认当前用户仍在该房间，再调用 `GET /api/friends` 拉取已通过申请的好友，逐个调用 `POST /api/invites/friend` 发送房间邀请，保留复制房间链接兜底。
- [x] 好友申请流程：好友必须通过搜索账号/昵称发申请、对方接受，或同房间成员发起申请并被接受后才成为好友；私聊和房间好友邀请都只允许已通过申请的好友。
- [x] 好友申请消息中心：收到好友申请时通过大厅 WebSocket 推送 `FRIEND_REQUEST_RECEIVED`，右上角消息角标 +1，消息中心通知 tab 直接显示接受/拒绝操作。
- [x] 好友同意后双端刷新：接受方立即重渲染右侧好友列表，申请方收到 `FRIEND_RELATION_UPDATED` 后自动刷新好友列表。
- [x] 刷新与退出当局：刷新不会因非鉴权错误直接退回登录页；有当前正式对局时直接恢复游戏，游戏中刷新/关闭会弹浏览器确认，HUD“退出当局”会调用后端退出接口并返回大厅。
- [x] 签到弹窗滚动：每日签到奖励列表改为自适应列数和内部滚动，滚动条复用房间列表样式，避免最后一个奖励项溢出。

---

## 1. 改造前代码结论

### 1.1 前端现状

- [x] 前端是 Vite + TypeScript，入口为 `src/main.ts`，当前直接渲染大厅 `renderLobbyPage()`。
- [x] 大厅主页面集中在 `src/lobby/lobby-page.ts`，包含顶部 tab、大厅列表、匹配、创建房间、加入房间、房间等待场景。
- [x] 当前没有真正的登录页面，启动时通过 `loginAsGuest()` 自动游客登录。
- [x] 当前房间 tab 的选中状态由 `activeRoom` 控制，一旦点击“返回大厅”会直接把 `activeRoom = null`，等同退出房间。
- [x] 当前点击房间列表会直接加入房间，不区分“进入房间页”和“加入玩家自建房间”。
- [x] 当前 `quickMatch()` 不是进入匹配队列，而是调用 `createRoom()` 创建一个公开房间。
- [x] 当前右侧房间页包含“房间信息、游戏设置、聊天区”，需要移除游戏设置，仅保留房间信息和聊天。
- [x] 当前房间玩家卡片是静态占位，没有根据 `room.members` 动态渲染。
- [x] 当前房间聊天是静态文字，没有接 API 或 WebSocket。
- [x] 当前好友列表是本地假数据，没有右键邀请好友、站内邀请、房间内拉好友的闭环。
- [x] 当前登录素材已有：`src/assets/logo/bj.png`，参考图位于根目录 `参考登录图.png`。

### 1.2 后端现状

- [x] 后端是 Spring Boot 多模块：`wildhunt-common`、`wildhunt-dal`、`wildhunt-service`、`wildhunt-web`。
- [x] 数据库表已包含用户、好友、房间、房间成员、邀请、匹配队列、对局、排行榜等基础表。
- [x] 但 `RoomService` 当前主要使用内存 `ConcurrentHashMap`，没有真实落库。
- [x] `RoomService.leave()` 当前只移除成员，房间空了仅把状态改为 `CLOSED`，没有从列表彻底排除或广播。
- [x] `MutableRoom.ownerUserId` 和 `MutableMember.owner` 都是 final，当前无法在房主退出时转移房主。
- [x] `MatchmakingService.enqueue()` 当前只是创建公开房间，不是真匹配。
- [x] `LobbyService.summary()` 队列数量固定返回 0，不读取真实匹配队列。
- [x] `FriendService`、`InviteService` 仍是占位实现。
- [x] `RoomWebSocketHandler` 当前只是广播原始消息，没有按房间隔离，也没有房间事件类型。
- [x] `AuthService` 只有游客登录，没有账号密码登录、注册、会话恢复、登录页业务。

---

## 2. 产品目标

### 2.1 房间目标

- [x] 顶部 tab 永远可以在“大厅”和“房间”之间切换。
- [x] 未加入任何房间时，点击“房间”tab 提示：`请先加入房间`。
- [x] 已加入或创建房间后，顶部“房间”tab 处于可访问状态。
- [x] 玩家在房间内点击“大厅”tab，只是切到大厅视图，不退出房间。
- [x] 玩家仍在房间但处于大厅视图时，顶部“房间”tab 下方显示房间标识，表示“你仍在房间内”。
- [x] 房间页按钮文案从“返回大厅”改为“退出房间”。
- [x] 只有点击“退出房间”才真正退出房间。
- [x] 房主退出时，房主转移给下一个进入房间的人。
- [x] 房间只有 1 人且该玩家退出时，房间销毁或关闭。
- [x] 房主可以踢人。
- [x] 房间内可以聊天。
- [x] 房间页右侧移除“游戏设置”，保留“房间信息”和“聊天区”。
- [x] 房间页中间显示好友/玩家卡片，使用当前好友卡片背景图风格。
- [x] 房间信息卡片、聊天区卡片收紧布局，避免溢出。

### 2.2 匹配目标

- [x] 系统匹配房间和玩家自建房间是两套概念。
- [x] 系统房间不显示在中间房间列表。
- [x] 玩家自建房间显示在中间房间列表。
- [x] 点击“匹配为狼”时，进入狼队列，优先寻找鹿队列玩家组成一局；没有鹿时用 AI 鹿补足目标。（2026-05-25 继续保留真人合流能力，且系统对局可 AI 补位）
- [x] 鹿玩家不足时，用 AI 鹿补足。（系统对局创建时写入 AI 鹿配置并生成 AI 玩家快照，`GameMatchService` 已持久化对局和真人玩家快照）
- [x] 点击“匹配为鹿”时，进入鹿队列，优先寻找狼队列玩家组成一局；没有真人狼时由服务端 AI 狼兜底，保证单人可玩。（2026-05-25 已改为直接匹配成功）
- [x] 狼玩家不足时，不在前端暴露“等待真人狼”内部状态；服务端用 AI 狼补位，好友侧只显示匹配/房间/游戏状态。
- [x] 点击右下角“开始游戏”时，作为自动补位进入队列，由后端随机分配狼或鹿，优先补当前缺人的系统匹配。
- [x] 系统匹配成功后直接进入对局或进入不可见系统房间的加载状态。
- [x] 玩家自建房间内邀请多个好友，一般只有 1 头狼，其他都是鹿，开局时角色随机。
- [x] 加入房间只针对玩家自建房间。

### 2.3 登录目标

- [x] 新增登录页面，不再启动后直接进入大厅。
- [x] 登录页面按根目录 `参考登录图.png` 的视觉方向制作。
- [x] 登录页背景使用 `src/assets/logo/bj.png`。
- [x] 登录页素材优先使用 `src/assets/logo` 下资源。
- [x] 登录页至少支持游客快速进入。
- [x] 登录页预留账号密码登录、注册、微信登录入口。
- [x] 登录成功后保存 token 和用户资料，再进入大厅。
- [x] 刷新页面时优先恢复本地 token；会话有效且有当前对局则直接恢复游戏，无当前对局则进入大厅；仅 401/403 鉴权失效才清 token 回登录页。

---

## 3. 数据库调整方案

### 3.1 `wh_room` 调整

当前字段基本够用，但需要补充房间类型、关闭时间、版本号和可见性语义。

```sql
ALTER TABLE wh_room
  ADD COLUMN room_type VARCHAR(32) NOT NULL DEFAULT 'PLAYER' COMMENT 'PLAYER 玩家自建房间，SYSTEM 系统匹配房间' AFTER status,
  ADD COLUMN closed_at DATETIME(3) NULL AFTER updated_at,
  ADD COLUMN version INT NOT NULL DEFAULT 0 AFTER closed_at;

CREATE INDEX idx_room_type_status ON wh_room (room_type, status, deleted);
CREATE INDEX idx_room_owner_status ON wh_room (owner_user_id, status, deleted);
```

字段约定：

- [x] `room_type = PLAYER`：玩家自建房间，显示在大厅房间列表。
- [x] `room_type = SYSTEM`：系统匹配房间，不显示在大厅房间列表。
- [x] `status = WAITING`：可加入。
- [x] `status = PLAYING`：已开局，不可加入。
- [x] `status = CLOSED`：已销毁或关闭，不显示。
- [x] `closed_at`：房间关闭时间。
- [x] `version`：并发加入、退出、开始游戏时做乐观锁或状态校验。

### 3.2 `wh_room_member` 调整

当前表有 `role_type`、`ready`、`owner_flag`、`connected`，还需要记录加入顺序、离开时间和是否被踢。

```sql
ALTER TABLE wh_room_member
  ADD COLUMN seat_no INT NULL AFTER user_id,
  ADD COLUMN joined_at DATETIME(3) NULL AFTER connected,
  ADD COLUMN left_at DATETIME(3) NULL AFTER joined_at,
  ADD COLUMN leave_reason VARCHAR(32) NULL AFTER left_at;

CREATE INDEX idx_room_member_active ON wh_room_member (room_id, deleted, left_at);
CREATE INDEX idx_room_member_user_active ON wh_room_member (user_id, deleted, left_at);
```

字段约定：

- [x] `owner_flag = 1`：当前房主。
- [x] `seat_no`：加入顺序，用于房主转移。
- [x] `left_at IS NULL AND deleted = 0`：当前仍在房间内。
- [x] `leave_reason = LEAVE`：主动退出。
- [x] `leave_reason = KICK`：被房主踢出。
- [x] `leave_reason = DISCONNECT_TIMEOUT`：断线超时离开。

### 3.3 `wh_match_queue` 调整

当前表有队列类型，但需要支持自动补位、队列批次和匹配结果。

```sql
ALTER TABLE wh_match_queue
  ADD COLUMN preferred_role VARCHAR(32) NOT NULL DEFAULT 'AUTO' AFTER queue_type,
  ADD COLUMN matched_room_id BIGINT NULL AFTER room_id,
  ADD COLUMN matched_match_id BIGINT NULL AFTER matched_room_id,
  ADD COLUMN matched_at DATETIME(3) NULL AFTER matched_match_id,
  ADD COLUMN cancelled_at DATETIME(3) NULL AFTER matched_at;

CREATE INDEX idx_queue_waiting_role ON wh_match_queue (status, preferred_role, created_at, deleted);
```

字段约定：

- [x] `queue_type` 保留兼容，可逐步统一为 `preferred_role`。
- [x] `preferred_role = WOLF`：用户明确匹配狼。
- [x] `preferred_role = DEER`：用户明确匹配鹿。
- [x] `preferred_role = AUTO`：右下角开始游戏，作为补位。
- [x] `status = WAITING`：仍在等待。
- [x] `status = MATCHED`：已匹配成功。
- [x] `status = CANCELLED`：用户取消或退出。
- [x] `matched_room_id`：系统房间 ID。
- [x] `matched_match_id`：真实对局 ID。

### 3.4 新增 `wh_room_chat_message`

房间聊天需要落库，WebSocket 实时广播，历史消息可分页加载。

```sql
CREATE TABLE IF NOT EXISTS wh_room_chat_message (
  id BIGINT PRIMARY KEY,
  room_id BIGINT NOT NULL,
  user_id BIGINT NULL,
  message_type VARCHAR(32) NOT NULL DEFAULT 'USER',
  content VARCHAR(500) NOT NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  KEY idx_room_chat_room_time (room_id, created_at, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

消息类型：

- [x] `USER`：玩家聊天。
- [x] `SYSTEM`：系统消息，例如加入、退出、房主转移、被踢。

### 3.5 新增 `wh_room_kick_log`

房主踢人需要可追踪，便于后续风控和客服处理。

```sql
CREATE TABLE IF NOT EXISTS wh_room_kick_log (
  id BIGINT PRIMARY KEY,
  room_id BIGINT NOT NULL,
  operator_user_id BIGINT NOT NULL,
  target_user_id BIGINT NOT NULL,
  reason VARCHAR(255) NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  KEY idx_room_kick_room (room_id, created_at),
  KEY idx_room_kick_target (target_user_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3.6 登录相关表调整

当前 `wh_user` 有 `password_hash`，可以支持账号密码登录，但建议补充登录时间、游客标识、登录方式。

```sql
ALTER TABLE wh_user
  ADD COLUMN user_type VARCHAR(32) NOT NULL DEFAULT 'GUEST' AFTER status,
  ADD COLUMN last_login_ip VARCHAR(64) NULL AFTER last_login_at;

CREATE INDEX idx_user_type_status ON wh_user (user_type, status, deleted);
```

字段约定：

- [x] `user_type = GUEST`：游客。
- [x] `user_type = ACCOUNT`：账号密码注册用户。
- [x] `user_type = WECHAT`：微信登录用户。

---

## 4. 后端业务调整方案

### 4.1 房间服务

目标服务：`RoomService`

- [x] 将当前内存 `Map<Long, MutableRoom>` 逐步替换为 MyBatis-Plus 落库实现。
- [x] 创建玩家自建房间时写入 `wh_room.room_type = PLAYER`。
- [x] 系统匹配创建房间时写入 `wh_room.room_type = SYSTEM`。
- [x] `listPublicRooms()` 只返回 `room_type = PLAYER AND status = WAITING AND deleted = 0`。
- [x] 用户同一时间只能在一个未关闭玩家房间内。
- [x] 创建房间前，如果用户已经在房间内，返回当前房间或提示先退出。
- [x] 加入房间前校验房间存在、未关闭、未开局、未满员。
- [x] 加入房间只允许加入 `PLAYER` 房间；系统房间只能由匹配服务写入。
- [x] 离开房间时写 `left_at`、`leave_reason = LEAVE`，并软删除当前成员关系。
- [x] 如果离开者是房主且房间仍有人，按 `seat_no ASC / joined_at ASC` 选择下一个成员为房主。
- [x] 如果房间没有成员，更新 `wh_room.status = CLOSED`、`closed_at = NOW()`。
- [x] 房主踢人时校验操作者是房主、目标是房间成员、不能踢自己。
- [x] 被踢成员写 `leave_reason = KICK`，插入 `wh_room_kick_log`。
- [x] 每次成员变化后广播 `ROOM_SNAPSHOT`。

新增/调整接口：

- [x] `GET /api/rooms/current`：获取当前用户所在房间，没有则返回 `null`。
- [x] `GET /api/rooms`：仅返回玩家自建房间。
- [x] `POST /api/rooms`：创建玩家自建房间。
- [x] `POST /api/rooms/{roomId}/join`：加入玩家自建房间。
- [x] `POST /api/rooms/{roomId}/leave`：退出房间。
- [x] `POST /api/rooms/{roomId}/kick`：房主踢人。
- [x] `POST /api/rooms/{roomId}/ready`：准备。
- [x] `POST /api/rooms/{roomId}/start`：玩家房间开局。

### 4.2 房间角色分配

目标服务：`RoomService.start()` / `GameMatchService`

- [x] 玩家自建房间开始游戏时，随机选 1 名真人为狼。
- [x] 其余真人全部为鹿。
- [x] 若房间只有 1 人，可以允许开局：该玩家为狼，鹿由 AI 补足；也可以禁止。当前按产品描述建议允许房主单人开局。
- [x] 写入 `wh_game_match`。（2026-05-24 已写入运行态 `GameMatchService`；真实表落库仍保留在落库重构项）
- [x] 写入 `wh_match_player`。（2026-05-24 已写入运行态玩家快照；真实表落库仍保留在落库重构项）
- [x] 更新 `wh_room.current_match_id`、`status = PLAYING`。
- [x] 广播 `GAME_START`，每个客户端只接收自己的 `assignedRole`。

### 4.3 系统匹配服务

目标服务：`MatchmakingService`

- [x] `QueueType.WOLF`：明确想当狼。
- [x] `QueueType.DEER`：明确想当鹿。
- [x] `QueueType.AUTO`：右下角开始游戏，作为补位。
- [x] 用户重复点击匹配时，不新增重复 `WAITING` 记录。
- [x] 用户已在玩家房间内时，点击匹配应提示先退出房间，或自动退出后再匹配。当前建议提示。
- [x] 狼队列有玩家时，优先匹配等待最久的鹿或 AUTO。
- [x] 鹿队列有玩家时，优先匹配等待最久的狼或 AUTO。
- [x] AUTO 队列优先补缺：缺狼则分配狼，缺鹿则分配鹿。
- [x] 成功匹配时创建 `SYSTEM` 房间，不出现在大厅房间列表。
- [x] 系统房间创建后立即创建对局，并为不足的鹿填充 AI 鹿数量。（2026-05-24 运行态对局和 AI 鹿快照已接入）
- [x] 匹配成功返回 `matchId`、`roomId`、`assignedRole`，前端直接进入游戏加载。
- [x] 匹配取消时更新 `status = CANCELLED`。（完成 100%：`MatchmakingService.cancel()` 会移除内存等待项并通过 `MatchQueueMapper` 将等待队列更新为 `CANCELLED`，无 Mapper 时保留本地兜底）

建议匹配规则第一版：

- [x] 一局至少 1 个狼方目标控制者：优先真人狼；鹿单人匹配时允许服务端 AI 狼控制，避免玩家一个人无法开始。
- [x] 鹿真人数量可以为 0，不足由 AI 鹿代替。
- [x] 如果用户点“匹配为狼”，没有鹿玩家也可以立即开局：该用户为狼，鹿全部 AI。
- [x] 如果用户点“匹配为鹿”，优先合流真人狼或 AUTO；当前没有可用真人狼时立即启用 AI 狼兜底，不向用户展示等待真人狼文案。
- [x] 如果用户点右下角“开始游戏”，后端按队列缺口分配；没有明显缺口时随机狼/鹿。

新增/调整接口：

- [x] `POST /api/matchmaking/queue`：入队，返回匹配状态或匹配结果。
- [x] `GET /api/matchmaking/status`：查询当前用户匹配状态。
- [x] `POST /api/matchmaking/cancel`：取消匹配。

### 4.4 房间聊天服务

新增服务：`RoomChatService`

- [x] `sendMessage(roomId, userId, content)` 校验用户在房间内。
- [x] 限制消息长度 1-500。
- [x] 限制发送频率，例如 1 秒 3 条。
- [x] 保存到 `wh_room_chat_message`。（完成 100%：`RoomChatService` 已通过 `RoomChatMessageMapper` 写入和读取历史消息，内存列表仅作为本地兜底）
- [x] 通过房间 WebSocket 广播 `ROOM_CHAT_MESSAGE`。
- [x] 加入房间时加载最近 30 条历史消息。
- [x] 系统消息也走同一张表，例如“玩家加入房间”、“房主已转移给 xxx”、“xxx 被房主移出房间”。

新增接口：

- [x] `GET /api/rooms/{roomId}/chat?limit=30`：获取房间聊天历史。
- [x] `POST /api/rooms/{roomId}/chat`：发送聊天消息，HTTP 兜底。

WebSocket 消息：

```json
{
  "type": "ROOM_CHAT_SEND",
  "roomId": "123",
  "content": "大家准备了吗"
}
```

```json
{
  "type": "ROOM_CHAT_MESSAGE",
  "roomId": "123",
  "message": {
    "id": "1",
    "userId": "10",
    "nickname": "Hunter_001",
    "messageType": "USER",
    "content": "大家准备了吗",
    "createdAt": "2026-05-24T20:00:00+08:00"
  }
}
```

### 4.5 好友邀请服务

目标服务：`FriendService`、`InviteService`

- [x] 好友列表返回真实好友、在线状态、是否游戏中。（当前为内存好友关系和在线态）
- [x] 房间内右键好友打开上下文菜单。
- [x] 菜单项包含：`邀请加入房间`、`查看资料`。
- [x] 邀请好友时校验当前用户在房间内。
- [x] 写入 `wh_room_invite`。（2026-05-24 当前为运行态邀请记录；真实表落库归入落库重构）
- [x] 给好友推送 `ROOM_INVITE_RECEIVED`。（当前通过通知闭环承载）
- [x] 好友接受邀请时调用加入玩家房间逻辑。
- [x] 如果房间已满、已开局、已关闭，接受邀请失败并提示。

新增/调整接口：

- [x] `GET /api/friends`：好友列表。
- [x] `GET /api/friends/search?keyword=`：按账号/昵称搜索可申请用户。
- [x] `GET /api/friends/requests`：查看收到/发出的好友申请。
- [x] `POST /api/friends/requests`：发起好友申请，不直接成为好友。
- [x] `POST /api/friends/requests/{requesterUserId}/accept|reject`：接受或拒绝好友申请。
- [x] `POST /api/friends/room-requests`：同房间成员互发好友申请，要求双方仍在同一房间。
- [x] `POST /api/invites/friend`：邀请好友加入当前房间。
- [x] `POST /api/invites/{inviteCode}/accept`：接受邀请。
- [x] `POST /api/invites/{inviteCode}/reject`：拒绝邀请。

### 4.6 登录业务

目标服务：`AuthService`、`UserService`、`JwtService`

- [x] 保留 `POST /api/auth/guest`。
- [x] 新增 `POST /api/auth/login`：账号密码登录。
- [x] 新增 `POST /api/auth/register`：注册账号。
- [x] 新增 `GET /api/auth/me`：根据 token 获取当前用户。
- [x] 新增 `POST /api/auth/logout`：前端清 token，后端可选记录。
- [x] 密码使用 BCrypt 哈希，不能明文保存。
- [x] 游客进入后可后续绑定账号，避免丢失进度。
- [x] JWT 包含 `userId`、过期时间、签名。
- [x] WebSocket 握手校验 token。

登录 DTO：

- [x] `LoginRequest(username, password)`
- [x] `RegisterRequest(username, password, nickname)`
- [x] `AuthSession(token, user)`
- [x] `UserProfile(userId, username, nickname, avatarUrl, rating, level, exp)`

---

## 5. 前端改造方案

### 5.1 路由和登录页

目标文件：

- [x] `src/main.ts`
- [x] `src/ui/router.ts`
- [x] `src/auth/login-page.ts`
- [x] `src/api/auth-api.ts`
- [x] `src/types/user.ts`
- [x] `src/style.css`

任务：

- [x] 新增简单路由状态：`login`、`lobby`、`game`。
- [x] 启动时调用 `getToken()`。
- [x] 有 token 时调用 `fetchMe()`。
- [x] `fetchMe()` 成功进入大厅。
- [x] 没有 token 或 token 失效进入登录页。
- [x] 登录页背景使用 `src/assets/logo/bj.png`。
- [x] 登录页视觉参考 `参考登录图.png`。
- [x] 登录页提供账号、密码输入框。
- [x] 登录页提供“登录”、“注册”、“游客进入”。
- [x] 登录成功后保存 token 和 user。
- [x] 登录失败显示 toast。

### 5.2 顶部 tab 状态

目标文件：

- [x] `src/lobby/lobby-page.ts`
- [x] `src/style.css`

新增前端状态：

```ts
type LobbyView = 'lobby' | 'room';

let currentView: LobbyView = 'lobby';
let activeRoom: RoomSnapshot | null = null;
```

任务：

- [x] 点击“大厅”tab：`currentView = 'lobby'`，不修改 `activeRoom`。
- [x] 点击“房间”tab：如果没有 `activeRoom`，toast `请先加入房间`。
- [x] 点击“房间”tab：如果有 `activeRoom`，`currentView = 'room'`。
- [x] `renderDynamicPanels()` 根据 `currentView` 决定显示大厅还是房间。
- [x] 顶部“房间”tab 下方显示房间标识，例如 `#1001` 或小点。
- [x] 如果玩家在房间内但当前看大厅，“大厅”tab active，“房间”tab 显示驻留标识。
- [x] 刷新页面时调用 `GET /api/rooms/current` 恢复 `activeRoom`。

### 5.3 房间页布局

目标文件：

- [x] `src/lobby/lobby-page.ts`
- [x] `src/style.css`

任务：

- [x] 把“返回大厅”按钮改成“退出房间”。
- [x] 点击“退出房间”调用 `leaveRoom(activeRoom.id)`。
- [x] 退出成功后 `activeRoom = null`，`currentView = 'lobby'`。
- [x] 移除右侧“游戏设置”卡片。
- [x] 右侧布局改为：房间信息 40%，聊天区 60%。
- [x] 聊天区增加消息列表和输入框。
- [x] 聊天列表固定高度，内部滚动，不撑破卡片。
- [x] 中间玩家/好友卡片根据 `activeRoom.members` 渲染。
- [x] 玩家卡片显示头像、昵称、房主标识、准备状态、角色占位。
- [x] 空位卡片显示“邀请好友”。
- [x] 房主视角下，非房主玩家卡片显示踢人按钮或右键菜单。
- [x] 非房主不能看到踢人操作。

### 5.4 房间列表

目标文件：

- [x] `src/lobby/room-list.ts`
- [x] `src/api/lobby-api.ts`

任务：

- [x] 去掉或隐藏 `sampleRooms`，避免系统假房间混入玩家自建房间列表。
- [x] 房间列表只展示后端 `GET /api/rooms` 返回的玩家自建房间。
- [x] 空列表显示“暂无玩家房间，创建一个邀请好友吧”。
- [x] 点击房间行或加入按钮调用 `joinRoom(roomId)`。
- [x] 加入成功后设置 `activeRoom`，`currentView = 'room'`。
- [x] 加入失败显示后端错误消息。

### 5.5 匹配按钮和右下角开始游戏

目标文件：

- [x] `src/lobby/match-panel.ts`
- [x] `src/lobby/lobby-page.ts`
- [x] `src/api/matchmaking-api.ts`

任务：

- [x] 新增 `src/api/matchmaking-api.ts`。
- [x] “匹配为狼”调用 `queueMatch('WOLF')`。
- [x] “匹配为鹿”调用 `queueMatch('DEER')`。
- [x] 右下角“开始游戏”调用 `queueMatch('AUTO')`。
- [x] 匹配中禁用三个按钮，显示匹配状态。（完成 100%：狼、鹿、右下角开始游戏按钮统一禁用，取消或失败后恢复）
- [x] 支持取消匹配按钮。
- [x] 匹配成功后保存 `matchId`、`roomId`、`assignedRole`。（完成 100%：前端写入 `sessionStorage.wildhunt.match.options` 并传入正式游戏入口）
- [x] 匹配成功后直接进入游戏场景。
- [x] 如果用户仍在玩家自建房间内，点击匹配提示先退出房间。

### 5.6 房间 WebSocket

目标文件：

- [x] `src/net/room-channel.ts`
- [x] `src/net/protocol.ts`
- [x] `src/lobby/lobby-page.ts`

任务：

- [x] 进入房间时连接 `/ws/room?roomId=...&token=...`。
- [x] 离开房间时关闭连接。
- [x] 收到 `ROOM_SNAPSHOT` 更新 `activeRoom`。
- [x] 收到 `ROOM_CHAT_MESSAGE` 追加聊天消息。
- [x] 收到 `ROOM_INVITE_RECEIVED` 显示邀请弹窗或 toast。（完成 100%：大厅 WebSocket 已接收邀请推送并刷新通知/未读数）
- [x] 收到 `ROOM_KICKED` 提示被移出房间，清空 `activeRoom`，回大厅。（完成 100%：后端定向推送，前端清空房间状态并回大厅）
- [x] 收到 `GAME_START` 进入游戏。（完成 100%：后端按玩家定向推送角色，前端通过房间 WS 进入正式游戏入口）

---

## 6. 接口清单

### 6.1 登录

- [x] `POST /api/auth/guest`
- [x] `POST /api/auth/login`
- [x] `POST /api/auth/register`
- [x] `GET /api/auth/me`
- [x] `POST /api/auth/logout`

### 6.2 大厅

- [x] `GET /api/lobby/summary`
- [x] `GET /api/rooms`
- [x] `GET /api/rooms/current`

### 6.3 玩家房间

- [x] `POST /api/rooms`
- [x] `POST /api/rooms/{roomId}/join`
- [x] `POST /api/rooms/join-by-code/{code}`
- [x] `POST /api/rooms/{roomId}/leave`
- [x] `POST /api/rooms/{roomId}/kick`
- [x] `POST /api/rooms/{roomId}/ready`
- [x] `POST /api/rooms/{roomId}/start`

### 6.4 房间聊天

- [x] `GET /api/rooms/{roomId}/chat`
- [x] `POST /api/rooms/{roomId}/chat`

### 6.5 匹配

- [x] `POST /api/matchmaking/queue`
- [x] `GET /api/matchmaking/status`
- [x] `POST /api/matchmaking/cancel`

### 6.6 好友和邀请

- [x] `GET /api/friends`
- [x] `GET /api/friends/search?keyword=`
- [x] `GET /api/friends/requests`
- [x] `POST /api/friends/requests`
- [x] `POST /api/friends/requests/{requesterUserId}/accept`
- [x] `POST /api/friends/requests/{requesterUserId}/reject`
- [x] `POST /api/friends/room-requests`
- [x] `POST /api/invites/friend`
- [x] `POST /api/invites/{inviteCode}/accept`
- [x] `POST /api/invites/{inviteCode}/reject`
- [x] `POST /api/invites/wechat`

---

## 7. WebSocket 事件清单

### 7.1 房间 WebSocket `/ws/room`

客户端发送：

- [x] `JOIN_ROOM`
- [x] `ROOM_CHAT_SEND`
- [x] `ROOM_READY_CHANGE`
- [x] `ROOM_LEAVE`

服务端发送：

- [x] `ROOM_SNAPSHOT`
- [x] `ROOM_CHAT_MESSAGE`
- [x] `ROOM_OWNER_TRANSFERRED`
- [x] `ROOM_MEMBER_JOINED`
- [x] `ROOM_MEMBER_LEFT`
- [x] `ROOM_KICKED`
- [x] `ROOM_CLOSED`
- [x] `GAME_START`
- [x] `ERROR`

### 7.2 大厅/通知 WebSocket

第一版可复用房间 WebSocket，后续建议新增 `/ws/lobby` 或 `/ws/notify`。

- [x] `LOBBY_SUMMARY_UPDATED`
- [x] `PLAYER_ROOM_LIST_UPDATED`
- [x] `ROOM_INVITE_RECEIVED`
- [x] `FRIEND_ONLINE_CHANGED`

---

## 8. 开发顺序和勾选任务

### 8.0 闭环业务补充：账号、成长、运营、排行榜

这一节用于补齐从登录到游玩再到长期留存的完整闭环。后续开发时不能只做“能进房间、能开局”，还需要让用户有身份、有成长、有奖励、有榜单、有通知、有活动入口。

#### 8.0.1 登录和用户信息闭环

- [x] 登录页支持游客进入、账号密码登录、账号注册。
- [x] 游客登录后生成临时账号，并写入 `wh_user`、`wh_player_profile`。
- [x] 游客可以后续绑定正式账号，绑定后保留等级、经验、奖杯、签到、背包、战绩。
- [x] 注册账号需要唯一用户名、昵称、密码。
- [x] 密码必须 BCrypt 哈希存储。
- [x] 登录成功返回 token、用户基础信息、玩家资料、未读通知数。
- [x] 刷新页面时通过 `GET /api/auth/me` 恢复用户信息。
- [x] 用户信息面板显示：头像、昵称、等级、经验条、奖杯数、胜场、总局数。
- [x] 用户头像支持默认头像、活动头像、后续上传头像。
- [x] 用户资料接口支持修改昵称、头像、个人称号。

建议接口：

- [x] `GET /api/users/me/profile`
- [x] `PUT /api/users/me/profile`
- [x] `GET /api/users/{userId}/profile`
- [x] `POST /api/auth/bind-account`

建议表调整：

```sql
ALTER TABLE wh_player_profile
  ADD COLUMN trophy INT NOT NULL DEFAULT 0 AFTER rating,
  ADD COLUMN total_matches INT NOT NULL DEFAULT 0 AFTER trophy,
  ADD COLUMN total_wins INT NOT NULL DEFAULT 0 AFTER total_matches,
  ADD COLUMN wolf_wins INT NOT NULL DEFAULT 0 AFTER total_wins,
  ADD COLUMN deer_wins INT NOT NULL DEFAULT 0 AFTER wolf_wins,
  ADD COLUMN best_wolf_win_seconds INT NULL AFTER deer_wins,
  ADD COLUMN best_deer_survive_seconds INT NULL AFTER best_wolf_win_seconds,
  ADD COLUMN avatar_frame VARCHAR(128) NULL AFTER title;
```

#### 8.0.2 等级、经验和成长规则

- [x] 等级由经验值驱动。
- [x] 每局结束后根据表现发放经验。
- [x] 每日签到发放固定经验。
- [x] 活动中心可发放经验、头像、头像框、称号等奖励。
- [x] 等级只作为匹配参考，不直接影响游戏数值，避免高等级玩家天然更强。
- [x] 等级经验曲线第一版可使用：`升级所需经验 = 100 + 当前等级 * 50`。
- [x] 等级上限第一版设置为 100。
- [x] 用户资料面板和右上角玩家卡片实时展示等级与经验进度。

经验来源建议：

- [x] 每日签到：+50 EXP。
- [x] 完成一局：+20 EXP。
- [x] 获胜：额外 +40 EXP。
- [x] 狼方淘汰真人鹿：每只 +15 EXP。
- [x] 鹿方存活到结束：+30 EXP。
- [x] 房间内邀请好友完成一局：+20 EXP。
- [x] 活动中心领取奖励：按活动配置发放。

#### 8.0.3 每日签到

每日签到不能只是静态按钮，需要有真实状态、奖励和领取记录。

- [x] 首页底部“每日签到”按钮打开签到弹窗。
- [x] 弹窗显示 7 日签到奖励。
- [x] 每天只能签到一次。
- [x] 连续签到天数影响奖励。
- [x] 签到奖励第一版以经验值为主。
- [x] 第 7 天可奖励头像框或称号。
- [x] 签到后刷新用户经验和等级。
- [x] 断签后连续天数重置，累计签到天数保留。

建议新增表：

```sql
CREATE TABLE IF NOT EXISTS wh_daily_checkin (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  checkin_date DATE NOT NULL,
  streak_days INT NOT NULL DEFAULT 1,
  reward_exp INT NOT NULL DEFAULT 0,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_checkin_user_date (user_id, checkin_date),
  KEY idx_checkin_user_time (user_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

建议接口：

- [x] `GET /api/checkin/status`
- [x] `POST /api/checkin/claim`
- [x] `GET /api/checkin/history`
- [x] `GET /api/admin/checkin/config`
- [x] `PUT /api/admin/checkin/config`
- [x] `GET /api/admin/checkin/users/{userId}/status`
- [x] `GET /api/admin/checkin/users/{userId}/records`
- [x] `POST /api/admin/checkin/users/{userId}/grant`
- [x] `POST /api/admin/checkin/users/{userId}/reset`
- [x] `POST /api/admin/checkin/reset-all`

#### 8.0.4 活动中心

活动中心也需要做活，不是普通跳转按钮。第一版可以做成可配置奖励列表。

- [x] 首页底部“活动中心”按钮打开活动中心弹窗或页面。
- [x] 活动包含标题、描述、奖励、开始时间、结束时间、领取条件、领取状态。
- [x] 第一版活动类型包含：登录领取、完成对局领取、邀请好友领取、胜利次数领取。
- [x] 活动奖励包含：经验、头像、头像框、称号、奖杯补给。
- [x] 领取奖励后写入用户资产或经验。
- [x] 已领取活动不可重复领取。
- [x] 过期活动不允许领取。

建议新增表：

```sql
CREATE TABLE IF NOT EXISTS wh_activity (
  id BIGINT PRIMARY KEY,
  activity_code VARCHAR(64) NOT NULL,
  title VARCHAR(128) NOT NULL,
  description VARCHAR(500) NULL,
  activity_type VARCHAR(32) NOT NULL,
  condition_value INT NOT NULL DEFAULT 1,
  condition_text VARCHAR(128) NULL,
  reward_json JSON NULL,
  starts_at DATETIME(3) NULL,
  ends_at DATETIME(3) NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_activity_code (activity_code),
  KEY idx_activity_status_time (status, starts_at, ends_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wh_activity_claim (
  id BIGINT PRIMARY KEY,
  activity_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  reward_json JSON NULL,
  claimed_at DATETIME(3) NOT NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_activity_claim_user (activity_id, user_id),
  KEY idx_activity_claim_user (user_id, claimed_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

建议接口：

- [x] `GET /api/activities`
- [x] `POST /api/activities/{activityId}/claim`
- [x] `GET /api/activities/my-progress`
- [x] `GET /api/admin/activities`
- [x] `POST /api/admin/activities`
- [x] `PUT /api/admin/activities/{activityId}`
- [x] `POST /api/admin/activities/{activityId}/disable`
- [x] `POST /api/admin/activities/claims/reset`

#### 8.0.4.1 赛季通行证

赛季通行证不能只是静态入口，需要有赛季等级、经验、奖励轨和领取记录。

- [x] 通行证等级由赛季经验计算，默认每 1000 EXP 提升 1 级，受 `season.pass.max_level` 限制。
- [x] 签到、活动领取和正式对局结算会推进赛季经验。
- [x] 免费轨和高级轨奖励由 `season.pass.free_rewards`、`season.pass.premium_rewards` 配置。
- [x] 奖励领取写入 `wh_season_pass_claim`，同一赛季同一轨道同一等级不可重复领取。
- [x] 高级轨未解锁时只展示锁定状态，不允许领取。
- [x] 后台支持查看/更新赛季配置、给用户补发赛季经验、解锁高级轨、重置用户通行证进度。

```sql
CREATE TABLE IF NOT EXISTS wh_season_pass_progress (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  season_code VARCHAR(32) NOT NULL,
  season_exp INT NOT NULL DEFAULT 0,
  level INT NOT NULL DEFAULT 1,
  premium_unlocked TINYINT NOT NULL DEFAULT 0,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_season_pass_progress_user (user_id, season_code),
  KEY idx_season_pass_progress_season (season_code, level, season_exp)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wh_season_pass_claim (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  season_code VARCHAR(32) NOT NULL,
  reward_track VARCHAR(16) NOT NULL,
  reward_level INT NOT NULL,
  reward_json JSON NULL,
  claimed_at DATETIME(3) NOT NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_season_pass_claim_user (user_id, season_code, reward_track, reward_level),
  KEY idx_season_pass_claim_user (user_id, season_code, claimed_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

- [x] `GET /api/season-pass`
- [x] `POST /api/season-pass/rewards/{track}/{level}/claim`
- [x] `POST /api/season-pass/premium/unlock`
- [x] `GET /api/admin/season-pass/config`
- [x] `PUT /api/admin/season-pass/config`
- [x] `GET /api/admin/season-pass/users/{userId}`
- [x] `POST /api/admin/season-pass/users/{userId}/exp`
- [x] `POST /api/admin/season-pass/users/{userId}/premium`
- [x] `POST /api/admin/season-pass/users/{userId}/reset`

#### 8.0.5 用户资产：头像、头像框、称号

- [x] 活动中心领取头像、头像框、称号后需要进入用户资产。
- [x] 用户资料页可以切换已拥有头像、头像框、称号。
- [x] 未拥有的头像和头像框不能设置。
- [x] 默认头像永久免费拥有。

建议新增表：

```sql
CREATE TABLE IF NOT EXISTS wh_user_asset (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  asset_type VARCHAR(32) NOT NULL,
  asset_code VARCHAR(128) NOT NULL,
  source VARCHAR(32) NOT NULL DEFAULT 'SYSTEM',
  obtained_at DATETIME(3) NOT NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE KEY uk_user_asset (user_id, asset_type, asset_code),
  KEY idx_user_asset_user (user_id, asset_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 8.0.6 新手指南

新手指南应该打开玩法指导，而不是空按钮。

- [x] 首页底部“新手指南”按钮打开新手指南弹窗或页面。
- [x] 指南内容包含：狼方目标、鹿方目标、AI 鹿伪装、房间玩法、系统匹配、技能说明、胜负规则、奖杯规则。
- [x] 指南支持分页或 tab。
- [x] 指南支持“我知道了”关闭。
- [x] 新用户首次进入大厅自动提示一次新手指南。
- [x] 用户关闭后记录 `guide_seen = true`，下次不自动弹。

建议表调整：

```sql
ALTER TABLE wh_player_profile
  ADD COLUMN guide_seen TINYINT NOT NULL DEFAULT 0 AFTER avatar_frame;
```

建议接口：

- [x] `POST /api/users/me/guide-seen`

#### 8.0.7 右上角通知

右上角消息按钮需要显示系统通知和社交通知。

通知类型建议：

- [x] `SYSTEM`：系统公告、维护通知、版本更新。
- [x] `ROOM_INVITE`：好友邀请加入房间。
- [x] `FRIEND_REQUEST`：好友申请。
- [x] `ACTIVITY_REWARD`：活动奖励可领取或已到账。
- [x] `RANK_CHANGE`：排行榜名次变化。
- [x] `MATCH_RESULT`：对局结算摘要。
- [x] `OWNER_TRANSFER`：房主转移通知。
- [x] `KICKED_FROM_ROOM`：被踢出房间通知。

任务：

- [x] 右上角通知按钮显示未读红点和数量。
- [x] 点击通知按钮打开通知面板。
- [x] 通知面板可标记单条已读。
- [x] 通知面板可一键全部已读。
- [x] 房间邀请通知可直接接受或拒绝。
- [x] 活动奖励通知可跳转活动中心。
- [x] 对局结果通知可查看结算详情。

建议调整现有 `wh_notification`：

```sql
ALTER TABLE wh_notification
  ADD COLUMN title VARCHAR(128) NULL AFTER type,
  ADD COLUMN read_at DATETIME(3) NULL AFTER read_flag;

CREATE INDEX idx_notification_user_time ON wh_notification (user_id, created_at, deleted);
```

建议接口：

- [x] `GET /api/notifications`
- [x] `GET /api/notifications/unread-count`
- [x] `POST /api/notifications/{notificationId}/read`
- [x] `POST /api/notifications/read-all`

#### 8.0.8 匹配算法闭环

后端匹配不能完全随机，需要避免 1 级新手和 100 级老玩家直接匹到一起。

匹配参考指标：

- [x] 等级 `level`。
- [x] 奖杯数 `trophy`。
- [x] 游玩次数 `total_matches`。
- [x] 近期胜率。
- [x] 角色偏好：狼、鹿、自动补位。

第一版匹配规则：

- [x] 优先按奖杯区间匹配。
- [x] 奖杯差距初始限制为 200。
- [x] 等级差距初始限制为 10 级。
- [x] 总局数少于 10 的新手进入新手保护池。
- [x] 新手保护池内优先匹配总局数少于 20 或等级低于 10 的玩家。
- [x] 等待超过 15 秒后放宽奖杯差距到 400。
- [x] 等待超过 30 秒后放宽等级差距到 25。
- [x] 等待超过 45 秒后允许更大范围匹配，但仍避免新手直接匹到 80 级以上玩家。
- [x] 狼方如果立即开 AI 鹿局，也要按狼玩家自身等级创建对应难度 AI 鹿。

建议 `MatchmakingService` 伪逻辑：

```text
1. 用户入队，读取 profile：level、trophy、totalMatches。
2. 判断是否新手池：level <= 10 或 totalMatches < 10。
3. 先找同池玩家。
4. 再按 trophy 差距排序。
5. 再按等待时间排序。
6. 组局时保证 1 狼，其余真人鹿，不足鹿用 AI 鹿。
7. 系统房间不进玩家房间列表。
```

#### 8.0.9 对局结果和奖杯规则

排行榜要有明确的“排什么”。建议第一主榜按奖杯数 `trophy` 排序，奖杯由每局表现计算。

对局结果必须写入：

- [x] 胜负方。
- [x] 游戏用时。
- [x] 狼是否获胜。
- [x] 鹿是否存活。
- [x] 狼击杀真人鹿数量。
- [x] 狼错杀 AI 鹿数量。
- [x] 鹿伪装时长。
- [x] 鹿被发现次数或可疑度峰值。
- [x] 鹿完成进食次数。
- [x] 每个玩家经验变化。
- [x] 每个玩家奖杯变化。

建议新增或调整 `wh_match_player` 字段：

```sql
ALTER TABLE wh_match_player
  ADD COLUMN trophy_delta INT NOT NULL DEFAULT 0 AFTER score_delta,
  ADD COLUMN exp_delta INT NOT NULL DEFAULT 0 AFTER trophy_delta,
  ADD COLUMN real_deer_kills INT NOT NULL DEFAULT 0 AFTER survived,
  ADD COLUMN ai_deer_wrong_kills INT NOT NULL DEFAULT 0 AFTER real_deer_kills,
  ADD COLUMN disguise_seconds INT NOT NULL DEFAULT 0 AFTER ai_deer_wrong_kills,
  ADD COLUMN grass_eat_count INT NOT NULL DEFAULT 0 AFTER disguise_seconds,
  ADD COLUMN suspicion_peak INT NOT NULL DEFAULT 0 AFTER grass_eat_count;
```

狼方奖杯建议：

- [x] 狼方获胜基础：+30 奖杯。
- [x] 狼方失败基础：-15 奖杯。
- [x] 每击杀 1 名真人鹿：+8 奖杯。
- [x] 胜利用时越短，额外奖杯越多：3 分钟内 +20，4 分钟内 +12，5 分钟内 +6。
- [x] 没有错杀 AI 鹿：+10 奖杯。
- [x] 每错杀 1 只 AI 鹿：-6 奖杯。
- [x] 狼方挂机或中途退出：-30 奖杯。

鹿方奖杯建议：

- [x] 鹿方获胜基础：+25 奖杯。
- [x] 鹿方失败基础：-10 奖杯。
- [x] 存活到结束：额外 +15 奖杯。
- [x] 伪装时长越长，额外奖杯越多：每 60 秒 +4，最多 +20。
- [x] 可疑度峰值低于 40：+10 奖杯。
- [x] 完成进食目标：+8 奖杯。
- [x] 被狼击杀：-5 奖杯。
- [x] 中途退出：-25 奖杯。

通用限制：

- [x] 奖杯不能低于 0。
- [x] 新手前 5 局失败扣杯减半。
- [x] 同一玩家每日可获得奖杯有上限，防止刷分。
- [x] 私人房间是否计入排行榜需要配置；第一版建议私人房间给经验，但不计奖杯主榜，避免刷榜。
- [x] 系统匹配房间计入奖杯主榜。

#### 8.0.10 排行榜闭环

排行榜不只显示一个列表。第一版大厅先落地两个玩家最容易理解、也最能支撑当前结算数据的榜单：奖杯榜和胜场榜。

第一版双榜：

- [x] 奖杯总榜：按 `trophy` 排。
- [x] 胜场榜：按 `total_wins` 排。

建议 `LeaderboardType` 扩展：

- [x] `TROPHY`
- [x] `WINS`

任务：

- [x] 每局结算后更新 `wh_player_profile.trophy`。
- [x] 每局结算后更新 `wh_player_stat`。
- [x] 每局结算后更新 `wh_leaderboard_entry`。
- [x] 大厅右侧排行榜默认显示奖杯总榜。
- [x] 排行榜支持奖杯榜/胜场榜切换。
- [x] 榜单显示玩家头像、昵称、等级、奖杯或对应指标。
- [x] 显示“我的排名”。
- [x] 好友榜只取好友关系内用户。

#### 8.0.11 游戏设置入口

顶部“设置”不能长期空置，但不应该出现在房间右侧。

- [x] 顶部“设置”打开设置弹窗。
- [x] 设置项包含：音量、画质、鼠标灵敏度、字体大小、UI 缩放、是否显示新手提示、退出登录。
- [x] 音频设置包含：主音量、背景音乐音量、音效音量、静音开关。
- [x] 画质设置包含：低、中、高、自定义。
- [x] 操作设置包含：鼠标灵敏度、键位说明、镜头反转开关。
- [x] 界面设置包含：字体大小、UI 缩放、聊天字号、是否显示房间信号说明。
- [x] 设置保存在 localStorage，必要时同步到后端。
- [x] 房间页右侧不再显示“游戏设置”卡片。

#### 8.0.12 房间列表信号图标

房间列表里的信号图标不能只是装饰，需要有明确含义。因为当前是网页游戏，玩家统一连接后端服务器，房间“信号”更适合表示这个房间当前的网络质量，而不是房主网络。

设计原则：

- [x] 信号图标代表房间实时网络质量。
- [x] 网络质量由后端根据房间成员 WebSocket 心跳统计计算。
- [x] 不建议永久落库，使用内存缓存即可。
- [x] REST 房间列表返回 `networkQuality` 字段。
- [x] 鼠标悬停信号图标显示 tooltip：平均延迟、最高延迟、丢包估计、更新时间。
- [x] 数据不足时显示灰色信号，文案为 `信号未知`。

信号等级建议：

- [x] 3 格绿色：`avgPingMs <= 80` 且 `maxPingMs <= 150`。
- [x] 2 格黄色：`avgPingMs <= 150` 或 `maxPingMs <= 250`。
- [x] 1 格红色：`avgPingMs > 150` 或 `maxPingMs > 250`。
- [x] 灰色：房间无在线 WebSocket 心跳或数据超过 15 秒未更新。

DTO 示例：

```ts
type RoomNetworkQuality = {
  level: 'GOOD' | 'NORMAL' | 'BAD' | 'UNKNOWN';
  avgPingMs: number | null;
  maxPingMs: number | null;
  onlineMembers: number;
  updatedAt: string | null;
};
```

任务：

- [x] 前端 `GameRoom` 增加 `networkQuality`。
- [x] 后端 `RoomSnapshot` 增加 `networkQuality`。
- [x] WebSocket 客户端每 10 秒发送 `PING`。
- [x] 服务端返回 `PONG` 并统计 RTT。
- [x] `GET /api/rooms` 聚合房间成员最近心跳生成信号等级。
- [x] 房间列表信号图标根据 `networkQuality.level` 渲染颜色和格数。

#### 8.0.13 好友右键、好友聊天和消息中心

好友系统需要分成三层：右键操作、好友私聊、右上角消息中心。

好友右键菜单：

- [x] 在大厅好友列表右键好友，打开上下文菜单。
- [x] 在房间内右键好友，菜单额外显示 `邀请加入房间`。
- [x] 菜单项包含：`查看基本信息`、`发送消息`、`邀请加入房间`、`删除好友`。
- [x] 未加入房间时不显示 `邀请加入房间`，或显示禁用态并提示 `请先加入房间`。
- [x] `查看基本信息` 打开用户资料弹窗。
- [x] 用户资料弹窗显示头像、昵称、等级、奖杯、总局数、胜率、狼胜场、鹿胜场、最近在线时间。
- [x] `发送消息` 打开好友聊天窗口。

好友聊天：

- [x] 好友聊天独立于房间聊天。
- [x] 房间聊天只在房间页右侧显示。
- [x] 好友聊天从右上角消息按钮进入，也可以从好友右键菜单进入。
- [x] 右上角消息中心包含通知 tab 和聊天 tab。
- [x] 收到的好友申请进入右上角消息中心通知 tab，角标计入待处理申请数。
- [x] 聊天 tab 显示最近会话列表、未读数、最后一条消息。
- [x] 点击会话打开私聊窗口。
- [x] 私聊支持文本消息，第一版不做图片和语音。
- [x] 私聊消息离线可读，用户上线后看到未读消息。

建议新增表：

```sql
CREATE TABLE IF NOT EXISTS wh_friend_chat_message (
  id BIGINT PRIMARY KEY,
  from_user_id BIGINT NOT NULL,
  to_user_id BIGINT NOT NULL,
  content VARCHAR(500) NOT NULL,
  read_flag TINYINT NOT NULL DEFAULT 0,
  read_at DATETIME(3) NULL,
  created_at DATETIME(3) NOT NULL,
  updated_at DATETIME(3) NOT NULL,
  deleted TINYINT NOT NULL DEFAULT 0,
  KEY idx_chat_from_to_time (from_user_id, to_user_id, created_at),
  KEY idx_chat_to_unread (to_user_id, read_flag, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

建议接口：

- [x] `GET /api/users/{userId}/profile`
- [x] `GET /api/chats/friends`
- [x] `GET /api/chats/friends/{friendUserId}/messages`
- [x] `POST /api/chats/friends/{friendUserId}/messages`
- [x] `POST /api/chats/friends/{friendUserId}/read`

WebSocket 事件：

- [x] `FRIEND_CHAT_MESSAGE`
- [x] `FRIEND_CHAT_READ`
- [x] `FRIEND_REQUEST_RECEIVED`
- [x] `FRIEND_RELATION_UPDATED`
- [x] `UNREAD_COUNT_UPDATED`

#### 8.0.14 背景音乐和音效资产管线

当前游戏缺少背景音乐和音效，需要建立音频资产管线。MiniMax 官方 Music Generation API 可用于生成音乐，官方文档示例接口为 `https://api.minimax.io/v1/music_generation`，示例模型为 `music-2.6`。API Key 不能写进文档、代码或 git，需要使用环境变量。

2026-05-25 复核更正：当前仓库已补入可运行的本地草稿音频资源，实际目录为 `public/audio`，格式为浏览器可直接播放的 WAV；MiniMax 正式生成版和人工试听选版仍不能算 100% 完成，必须等真实 provider 导出与试听确认后再勾选。

安全要求：

- [x] 不把 API Key 写入仓库。
- [x] 不把 API Key 写入本文档。
- [x] 本地只通过环境变量 `MINIMAX_API_KEY` 接收 provider key；当前仓库不包含 key。
- [x] 如果 key 曾经出现在聊天或截图中，建议在平台后台轮换。
- [x] 生成脚本只读取环境变量。

实际目录：

```text
public/audio
├── music
│   ├── lobby_loop.wav
│   ├── room_loop.wav
│   ├── game_wolf_tension_loop.wav
│   ├── game_deer_stealth_loop.wav
│   ├── match_start_stinger.wav
│   ├── victory_stinger.wav
│   └── defeat_stinger.wav
└── sfx
    ├── ui_click.wav
    ├── ui_confirm.wav
    ├── notification.wav
    ├── invite_received.wav
    ├── match_found.wav
    ├── room_join.wav
    ├── room_leave.wav
    ├── ready_toggle.wav
    ├── game_start.wav
    ├── wolf_scent.wav
    ├── wolf_bite.wav
    ├── wrong_ai_deer.wav
    ├── deer_eat.wav
    ├── deer_disguise.wav
    ├── level_up.wav
    ├── checkin_claim.wav
    └── activity_claim.wav
```

音乐需求：

- [x] 大厅音乐：轻松、森林、卡通、循环播放，时长 60-90 秒。
- [x] 房间音乐：等待感、轻微鼓点、不过分紧张，循环播放。
- [x] 游戏内狼方音乐：追猎、低鼓、紧张但不卡通割裂，循环播放。
- [x] 游戏内鹿方音乐：潜行、紧张、轻快木琴或拨弦，循环播放。
- [x] 开始游戏音乐：2-4 秒短促 stinger。
- [x] 结算胜利音乐：4-8 秒积极反馈。
- [x] 结算失败音乐：4-8 秒轻微失落但不阴暗。

音效需求：

- [x] UI 点击。
- [x] 匹配成功。
- [x] 收到好友消息。
- [x] 收到房间邀请。
- [x] 创建房间。
- [x] 加入房间。
- [x] 退出房间。
- [x] 准备状态切换。
- [x] 房主开始游戏。
- [x] 狼嗅觉技能。
- [x] 狼攻击。
- [x] 错杀 AI 鹿惩罚。
- [x] 鹿吃草。
- [x] 鹿伪装。
- [x] 每日签到领奖。
- [x] 活动奖励领取。
- [x] 升级。

前端音频任务：

- [x] 新增 `src/audio/audio-manager.ts`。
- [x] 支持播放 BGM、切换 BGM、停止 BGM。
- [x] 支持播放一次性 SFX。
- [x] 支持主音量、BGM 音量、SFX 音量。
- [x] 支持静音。
- [x] 设置面板修改音量后立即生效。
- [x] 浏览器首次交互后再启动音频，避免自动播放被拦截。
- [x] 房间页播放 `room_loop`。
- [x] 大厅播放 `lobby_loop`。
- [x] 游戏内根据角色播放狼方或鹿方循环音乐。
- [x] 结算时淡出游戏音乐并播放胜负 stinger。

生成脚本任务：

- [x] 新增 `scripts/generate-audio-assets.mjs`。
- [x] 脚本检测 `MINIMAX_API_KEY`，但当前默认生成本地草稿 WAV 资源，避免把 key 写入仓库。
- [x] 根据内置音色 specs 批量生成候选音乐和音效草稿。
- [ ] 下载为 wav/mp3 后转码为 ogg。（完成 40%：已有 WAV 资源；OGG 转码链路和 provider 下载尚未接入）
- [x] 对循环音乐做淡入淡出和 loop 检查。
- [x] 对短音效裁剪到 0.2-2 秒。
- [ ] 生成后人工试听，选中版本再提交。（完成 60%：已生成可播放资源并接入前端；仍缺人工试听确认和最终替换版）

#### 8.0.15 开局加载、Label 和开始动画

游戏不能在点击开始后突然进入可操作状态。当前还存在模型或地形未加载完成时就开始的问题，需要增加正式的开局过渡流程。

目标体验：

- [x] 房主点击开始或系统匹配成功后，先进入加载界面。
- [x] 加载界面显示 `正在加载地形`、`正在加载角色`、`正在同步玩家`、`正在准备音频` 等状态。
- [x] 所有关键资源加载完成后，显示开局 Label，例如 `荒野追猎开始`、`你是狼`、`你是鹿`。
- [x] 开局 Label 后显示 3 秒倒计时：`3 / 2 / 1 / 开始`。
- [x] 倒计时期间玩家不能移动、不能攻击、不能使用技能。
- [x] 倒计时结束后才开启输入、计时器和服务端对局状态。
- [x] 音乐在加载结束后淡入，开始瞬间播放 `game_start` 音效。
- [x] 如果加载超过 15 秒，显示重试或返回大厅/房间按钮。

前端状态机建议：

```ts
type GameBootState =
  | 'MATCHED'
  | 'LOADING_SCENE'
  | 'LOADING_MODELS'
  | 'LOADING_AUDIO'
  | 'SYNCING_PLAYERS'
  | 'READY'
  | 'INTRO'
  | 'COUNTDOWN'
  | 'PLAYING'
  | 'LOAD_FAILED';
```

资源加载要求：

- [x] 地形、树木、草、石头等场景资源加载完成。
- [x] 狼模型加载完成。
- [x] 鹿模型加载完成。
- [x] 玩家自己角色模型加载完成。
- [x] 其他真人玩家占位模型加载完成。
- [x] AI 鹿必要模型加载完成。
- [x] HUD 初始化完成。
- [x] BGM 和关键音效预加载完成或确认可降级播放。
- [x] WebSocket 已收到服务端 `GAME_READY` 或首个 `GAME_SNAPSHOT`。

后端协作：

- [x] 房主开始游戏后，后端先创建 match，但状态为 `LOADING` 或 `CREATED`。
- [x] 客户端加载完成后发送 `CLIENT_READY`.
- [x] 服务端收齐必要玩家 ready，或超过最大等待时间后，广播 `GAME_COUNTDOWN`。
- [x] 服务端广播倒计时结束时刻 `startAt`，各客户端按同一时间开局。
- [x] 服务端在 `startAt` 后才接受有效输入。
- [x] 超时未 ready 玩家按断线或旁观处理，第一版可踢回房间。

WebSocket 事件：

- [x] `GAME_LOADING`
- [x] `CLIENT_READY`
- [x] `GAME_READY`
- [x] `GAME_COUNTDOWN`
- [x] `GAME_STARTED`
- [x] `GAME_LOAD_FAILED`

前端任务：

- [x] 新增 `src/game/game-loader.ts` 管理资源预加载。
- [x] 新增 `src/game/game-intro.ts` 管理开局 Label 和倒计时动画。
- [x] `startLocalGame()` 或正式进入游戏流程不能直接开启输入。
- [x] 加载期间展示进度和当前阶段文案。
- [x] 模型和地形未加载完成时不显示空场景。
- [x] 倒计时结束前禁用移动、攻击、技能、计时器。
- [x] 开局动画适配狼/鹿不同角色文案。
- [x] 开局动画结束后再显示完整 HUD。

建议开局文案：

- [x] 狼方：`你是狼`、`找出所有伪装的鹿`、`追猎开始`。
- [x] 鹿方：`你是鹿`、`混入鹿群并活到最后`、`伪装开始`。
- [x] 通用：`荒野追猎开始`、`3`、`2`、`1`、`开始`。

#### 8.0.16 前端游戏代码对齐和联机迁移闭环

当前 `src/game/game-scene.ts` 仍是单机原型：本地随机生成地形/鹿群/食物，本地选择阵营，本地判定命中和结算。正式业务必须把它改造成“服务端开局、服务端配置、服务端结算，前端负责表现和输入”的结构。

当前代码发现：

- [x] `game-scene.ts` 文件过大，约 90KB，场景、输入、AI、技能、HUD、结算都在一个文件里。
- [x] `game-config.ts` 已有 `MAX_ROOM_PLAYERS`、`WOLF_COUNT`、`DEFAULT_AI_DEER_COUNT`、`DEV_LOCAL_MODE`，但 `game-scene.ts` 内又硬编码 `REAL_DEER_COUNT = 4`、`AI_DEER_COUNT = 16`。
- [x] 当前通过 `localStorage.wildhunt.assignedRole` 和 `wildhunt.skipRoleDialog` 控制本地角色。
- [x] 当前仍有本地“选择阵营”弹窗，正式联机不应允许玩家自行切换狼/鹿。
- [x] 当前 `loadNatureModels()` 和 `loadGameModels()` 没有返回 Promise，模型还没加载完就会开局。
- [x] 当前 `loadModel()` 没有错误兜底，模型失败时无法明确反馈。
- [x] 当前地图、食物点、AI 鹿、真人鹿标记都由客户端随机生成，多人客户端不会一致。
- [x] 当前鹿方模式里的本地 `updateEnemyWolfAI()` 只作为前端表现和本地模式兜底；正式对局胜负、技能和 `MATCH_END` 以后端权威状态为准。
- [x] 当前狼扑咬、错杀 AI 鹿、气味追踪、鹿伪装、环顾、进食都在客户端本地判定。
- [x] 当前 `finishRound()` 只弹本地结果，没有向后端提交结算，也没有经验/奖杯/排行榜闭环。
- [x] 当前 `game-scene.ts` 已消费正式 `GAME_SNAPSHOT`，并在收到首帧后才解除正式对局加载闸门。
- [x] 当前 `protocol.ts` 已补齐房间、邀请、`GAME_START`、`GAME_SNAPSHOT`、`MATCH_END` 和带 `seq` 的 `PLAYER_INPUT` 类型；服务端权威技能事件仍在正式对局判定项下跟踪。
- [x] 当前 debug 碰撞显示可用 `F` 开关，正式环境需要隐藏或只在开发模式启用。

正式入口约束：

- [x] 不再通过 `localStorage` 决定正式角色。
- [x] 正式游戏入口必须传入 `matchId`、`roomId`、`assignedRole`、`matchSeed`、`gameConfig`。
- [x] 只有后端 `GAME_START` 或匹配结果可以进入正式游戏。
- [x] 本地选阵营弹窗只允许在 `DEV_LOCAL_MODE = true` 时使用。
- [x] 生产构建默认 `DEV_LOCAL_MODE = false`。
- [x] `restart-btn` 在正式对局结算后不再打开选阵营，而是显示：返回房间、再匹配、返回大厅。

建议正式入口 API：

```ts
type GameLaunchOptions = {
  mode: 'ONLINE' | 'DEV_LOCAL';
  matchId: string;
  roomId?: string;
  assignedRole: 'WOLF' | 'DEER';
  matchSeed: string;
  config: GameRuntimeConfig;
};

export async function mountGameScene(options: GameLaunchOptions): Promise<GameSceneController>;
```

文件拆分任务：

- [x] `src/game/game-scene.ts` 只保留装配和主循环。
- [x] 新增 `src/game/game-loader.ts`：地形、模型、自然物、音频预加载。
- [x] 新增 `src/game/game-runtime-config.ts`：统一玩法配置。
- [x] 新增 `src/game/game-world.ts`：地形、植被、食物点、碰撞体。
- [x] 新增 `src/game/game-actors.ts`：狼、鹿、远端玩家、AI 鹿创建。
- [x] 新增 `src/game/game-actions.ts`：技能表现和本地输入映射。
- [x] 新增 `src/game/game-network.ts`：输入上报、快照接收、插值。
- [x] 新增 `src/game/game-result.ts`：结算 UI 和回大厅/房间/再匹配。
- [x] 新增 `src/game/game-dev-local.ts`：单机调试模式。

服务端配置下发：

- [x] `INITIAL_TIME`、`MAX_TIME` 从后端配置下发。
- [x] `REAL_DEER_COUNT` 不再写死，等于本局真人鹿玩家数量。
- [x] `AI_DEER_COUNT` 从房间或匹配结果下发。
- [x] `CORRECT_BONUS`、`WRONG_PENALTY` 从后端配置下发。
- [x] 狼/鹿速度、冲刺、技能冷却、嗅觉次数、伪装时长、进食速度从后端配置下发。
- [x] 前端只做展示和预测，关键数值以后端为准。

随机和地图一致性：

- [x] 每局由后端生成 `matchSeed`。
- [x] 前端所有随机点位改为 seeded RNG。
- [x] 地形可继续使用确定性函数，但食物点、AI 鹿初始点、玩家出生点必须由 `matchSeed` 或后端列表决定。
- [x] 后端下发 `spawnPoints`、`foodPatches`、`aiDeerSeeds`。
- [x] 所有客户端看到相同 AI 鹿、食物点和场景关键布局。
- [x] 如果服务端权威 AI 鹿压力过大，第一版可让客户端用相同 seed 本地模拟 AI 鹿，但服务端必须掌握真人鹿位置和胜负判定。

输入帧协议：

```ts
type PlayerInputFrame = {
  seq: number;
  clientTime: number;
  moveAxis: -1 | 0 | 1;
  turnAxis: -1 | 0 | 1;
  sprint: boolean;
  action:
    | 'NONE'
    | 'WOLF_POUNCE'
    | 'WOLF_SCENT'
    | 'DEER_EAT'
    | 'DEER_LOOK'
    | 'DEER_CAMOUFLAGE';
  yaw: number;
};
```

- [x] 前端以固定频率发送输入帧，例如 20Hz。
- [x] 一次性技能动作发送后需要带 `seq`，避免重复触发。
- [x] 服务端校验技能冷却和角色权限。
- [x] 前端可以做本地预测，但服务端快照要能纠偏。

快照协议：

```ts
type GameSnapshot = {
  tick: number;
  serverTime: number;
  matchTimeLeft: number;
  players: Array<{
    userId: string;
    role: 'WOLF' | 'DEER';
    x: number;
    y: number;
    z: number;
    yaw: number;
    speed: number;
    animation: 'idle' | 'walk' | 'gallop' | 'eat' | 'attack' | 'death';
    alive: boolean;
  }>;
  aiDeer?: Array<{
    id: string;
    x: number;
    y: number;
    z: number;
    yaw: number;
    state: 'wander' | 'pause' | 'graze' | 'look' | 'startled' | 'dead';
  }>;
  self?: {
    stamina: number;
    hunger?: number;
    suspicion?: number;
    scentUsed?: boolean;
    cooldowns: Record<string, number>;
  };
};
```

- [x] 服务端 10-20Hz 下发 `GAME_SNAPSHOT`。
- [x] 前端对远端玩家做插值，不直接硬跳位置。
- [x] 自己玩家可以预测移动，但超过阈值要平滑纠偏。
- [x] AI 鹿可以低频同步，前端插值或本地模拟。
- [x] HUD 的倒计时以后端 `matchTimeLeft` 为准。

技能和结果事件：

- [x] 狼气味不由前端 `nearestRealDeer()` 判定，改为请求服务端返回方向和强度。
- [x] 狼扑咬命中不由前端最终决定，服务端返回 `BITE_HIT_REAL_DEER`、`BITE_HIT_AI_DEER`、`BITE_MISS`。
- [x] 鹿环顾由服务端返回狼的大致方向和距离段。
- [x] 鹿进食由服务端校验是否靠近食物点，并更新饥饿值。
- [x] 鹿伪装由服务端校验静止状态、距离和冷却。
- [x] 前端技能粒子、提示、动画只响应服务端事件或本地预测确认。
- [x] `MATCH_END` 必须包含胜负、经验变化、奖杯变化、排行榜变化摘要。

正式结算 UI：

- [x] 结算弹窗显示胜负、角色、用时、击杀真人鹿、错杀 AI 鹿、伪装时长、进食次数、可疑度峰值。
- [x] 显示经验增加、是否升级、奖杯变化、当前奖杯。
- [x] 显示按钮：返回房间、再匹配、返回大厅。
- [x] 私人房间结算说明：经验到账，奖杯是否计入按配置展示。
- [x] 系统匹配结算说明：奖杯计入排行榜。

资源加载闭环：

- [x] 所有 `loader.load` 封装为 Promise。
- [x] `loadNatureModels()` 必须 await。
- [x] `loadGameModels()` 必须 await 狼和鹿模型。
- [x] 资源加载失败时保留程序化模型兜底，并显示低干扰提示。
- [x] 加载界面显示每类资源进度。
- [x] 进入 `COUNTDOWN` 前，模型、地形、HUD、音频、WebSocket 首帧必须完成。

开发调试边界：

- [x] `F` 碰撞调试只在 `DEV_LOCAL_MODE` 或开发环境启用。
- [x] debug 文本默认隐藏。
- [x] 本地 AI 狼在 `DEV_LOCAL_MODE` 中负责完整本地玩法；正式对局允许作为视觉表现，但不能私自结算，结果必须来自服务端 `MATCH_END`。
- [x] 单机 `resetRound()` 只用于开发调试，不参与正式经验、奖杯、排行榜。
- [x] 正式对局不能让客户端自行改 `assignedRole`。

#### 8.0.17 手机横屏适配闭环

本项目需要支持手机横屏游玩，不能只按桌面端设计。移动端优先适配横屏，竖屏可以显示引导用户旋转屏幕的提示。

目标设备范围：

- [x] iPhone 主流尺寸横屏。
- [x] Android 主流 16:9、19.5:9、20:9 横屏。
- [x] 小屏宽度约 667px 到 932px 的横屏。
- [x] 平板横屏可按桌面紧凑布局处理。
- [x] 竖屏不强行塞完整游戏 UI，优先显示旋转提示。

全局适配原则：

- [x] 使用 `safe-area-inset-*` 处理刘海屏和圆角屏安全区。
- [x] 禁止页面横向滚动。
- [x] 游戏画布全屏铺满，不出现黑边。
- [x] 所有按钮触控区域不小于 44px。
- [x] 文字不依赖 `vw` 无限缩放，使用稳定字号和容器约束。
- [x] 弹窗最大高度不能超过视口，内容可内部滚动。
- [x] 手机横屏下不显示 hover-only 信息，tooltip 需要可点按展开。

登录页横屏：

- [x] 背景图铺满横屏，不拉伸变形。
- [x] 登录面板靠右或居中偏右，避免遮挡核心视觉。
- [x] 输入框、登录、注册、游客按钮高度适合触控。
- [x] 小屏横屏时隐藏非必要装饰，只保留登录功能。
- [x] 软键盘弹出时表单不被遮挡。

大厅横屏：

- [x] 顶部 logo、tab、玩家卡片压缩为一行紧凑布局。
- [x] 底部 dock 在手机横屏改为右侧或底部紧凑工具栏。
- [x] 匹配按钮、房间列表、好友/排行榜需要重排，避免三栏同时挤爆。
- [x] 推荐横屏手机大厅布局：左侧匹配区，中间房间列表，右侧折叠为消息/好友抽屉。
- [x] 排行榜、好友列表、活动中心、签到、新手指南在手机横屏以弹层或抽屉打开。
- [x] 房间列表每行高度固定，加入按钮可触控。
- [x] 信号图标点按显示网络质量详情。

房间页横屏：

- [x] 房间玩家卡片横向滚动或两行网格展示。
- [x] 房间信息收缩为顶部/侧边小面板。
- [x] 聊天区默认折叠为按钮，点开后从右侧抽屉滑出。
- [x] 房主踢人按钮必须可触控，不依赖右键。
- [x] 好友邀请不依赖右键，手机上通过长按或更多按钮打开操作菜单。
- [x] 退出房间按钮放在安全区内，避免误触系统手势区域。

游戏横屏 HUD：

- [x] 画布全屏。
- [x] 左下角为虚拟摇杆或方向按钮。
- [x] 右下角为技能按钮组。
- [x] 顶部中间显示倒计时。
- [x] 左上显示角色和关键状态。
- [x] 狼方显示：目标鹿、错杀 AI、体力、气味次数。
- [x] 鹿方显示：饥饿、可疑度、狼距离提示、技能冷却。
- [x] 技能按钮大小不小于 52px。
- [x] 倒计时和开局 Label 不遮挡核心视野。
- [x] 结算弹窗横屏居中，按钮横向排列。
- [x] 手机横屏下隐藏 debug 文本。
- [x] 低端手机默认降低阴影、SSAO、植被密度或渲染分辨率。

移动端操作：

- [x] 支持触摸虚拟摇杆移动。
- [x] 支持按钮触发冲刺、气味、扑咬、进食、环顾、伪装。
- [x] 技能按钮要显示冷却遮罩。
- [x] 触摸时禁止页面滚动和浏览器缩放。
- [x] 支持多点触控：左手移动，右手技能。
- [x] 断触、切后台、来电、锁屏时自动清空输入。
- [x] 手机横屏不依赖键盘操作。

移动端性能：

- [x] 检测移动端后降低 `renderer.setPixelRatio` 上限，例如不超过 1.5。
- [x] 移动端默认关闭或降低 SSAO。
- [x] 移动端降低阴影贴图尺寸。
- [x] 移动端减少远处草、树和灌木数量。
- [x] 移动端限制粒子数量，例如气味粒子减少 30%-50%。
- [x] 移动端目标帧率至少 30 FPS。
- [x] 提供画质设置：省电、均衡、高清。

CSS 断点建议：

```css
@media (orientation: landscape) and (max-height: 520px) {
  /* 手机横屏布局 */
}

@media (orientation: portrait) and (max-width: 820px) {
  /* 竖屏旋转提示 */
}
```

前端任务：

- [x] 新增移动端检测工具 `src/ui/device.ts`。
- [x] 新增横屏旋转提示组件。
- [x] 新增手机大厅横屏布局样式。
- [x] 新增手机房间横屏布局样式。
- [x] 新增游戏虚拟摇杆组件。
- [x] 新增移动端技能按钮冷却表现。
- [x] 设置面板增加移动端画质预设。
- [x] Playwright 或浏览器手工验证 iPhone/Android 横屏尺寸。

验收尺寸建议：

- [x] `667 x 375`，小屏 iPhone 横屏。
- [x] `844 x 390`，常见 iPhone 横屏。
- [x] `932 x 430`，大屏 iPhone 横屏。
- [x] `915 x 412`，常见 Android 横屏。
- [x] `1024 x 768`，平板横屏。

#### 8.0.18 全链路保障：权限、事务、幂等、配置、恢复

这部分不是新玩法，但决定前后端是否真正闭环。所有“点击一次可以成功”的功能，都要考虑重复点击、刷新、断线、重连、并发、权限和失败兜底。

权限矩阵：

- [x] 未登录用户只能访问登录、注册、游客登录、公开静态资源。
- [x] 登录用户可以访问大厅、玩家房间列表、好友、通知、签到、活动。
- [x] 只有房间成员可以访问房间详情、房间聊天、房间 WebSocket。
- [x] 只有房主可以开始房间、踢人。
- [x] 只有对局成员可以连接游戏 WebSocket。
- [x] 只有自己可以领取自己的签到和活动奖励。
- [x] 只有好友关系双方可以发起好友私聊。
- [x] 管理类活动配置不在玩家接口暴露，第一版先用数据库种子或后端配置维护。

事务边界：

- [x] 创建房间：`wh_room`、`wh_room_member` 必须同事务。
- [x] 加入房间：校验容量、写成员、写系统聊天、发通知或广播，核心数据同事务。
- [x] 退出房间：成员离开、房主转移或房间关闭、系统消息同事务。
- [x] 踢人：权限校验、成员离开、kick log、系统消息同事务。
- [x] 开始房间：房间状态、角色分配、match、match_player 同事务。
- [x] 匹配成功：队列状态、系统房间、match、match_player 同事务。
- [x] 结算：match 状态、match_player、player_profile、player_stat、leaderboard、notification 同事务。
- [x] 签到领取：签到记录、经验、等级、通知同事务。
- [x] 活动领取：领取记录、资产或经验、通知同事务。

幂等和重复点击：

- [x] 登录和注册按钮需要前端 loading 防重复。
- [x] 创建房间重复点击不能创建多个房间。
- [x] 加入房间重复点击不能插入重复成员。
- [x] 退出房间重复点击第二次应返回已退出或当前无房间。
- [x] 踢人重复点击第二次应返回目标已不在房间。
- [x] 开始游戏重复点击只能创建一场 match。
- [x] 匹配入队重复点击不能产生多条等待队列。
- [x] 取消匹配重复点击应安全返回。
- [x] 每日签到重复领取应返回今日已签到。
- [x] 活动奖励重复领取应返回已领取。
- [x] `MATCH_END` 重复到达时结算只能执行一次。
- [x] WebSocket 输入帧按 `seq` 去重，过旧输入丢弃。

错误码和前端提示：

- [x] 后端统一错误码，不只返回 `HTTP 500`。
- [x] 前端把常见错误转成用户能理解的 toast。
- [x] `UNAUTHORIZED`：请重新登录。
- [x] `ROOM_NOT_FOUND`：房间不存在或已关闭。
- [x] `ROOM_FULL`：房间已满。
- [x] `ROOM_ALREADY_STARTED`：房间已开始游戏。
- [x] `NOT_ROOM_OWNER`：只有房主可以操作。
- [x] `ALREADY_IN_ROOM`：你已在房间内。
- [x] `MATCH_ALREADY_QUEUED`：你已在匹配队列中。
- [x] `REWARD_ALREADY_CLAIMED`：奖励已领取。
- [x] `CHAT_RATE_LIMITED`：发送太快了。

限流和风控：

- [x] 登录失败限流，防暴力猜密码。
- [x] 注册限流，防批量刷账号。
- [x] 聊天限流，每秒最多 3 条。
- [x] 好友申请限流。
- [x] 房间邀请限流。
- [x] 匹配入队/取消限流。
- [x] 昵称、房间名、聊天内容需要敏感词或基础脏词过滤。
- [x] 对局异常输入、速度跳变、技能频率异常写入 `wh_match_event`。
- [x] 奖杯和活动奖励需要防刷规则。

配置和默认数据：

- [x] 玩法数值放入 `wh_system_config` 或后端配置类，前端不硬编码正式数值。（完成 100%：新增 `SystemConfigService` 聚合读取 `wh_system_config`，`GameMatchService` 统一输出 `gameConfig`，V3 迁移补齐游戏时长、半径、速度、技能冷却、食物和自然物密度等默认值；前端正式入口继续通过 `normalizeRuntimeConfig()` 消费）
- [x] 签到 7 日奖励需要默认数据。
- [x] 活动中心需要默认活动数据。
- [x] 默认头像、头像框、称号需要默认资产配置。
- [x] 排行榜赛季需要默认赛季。
- [x] 新手指南版本号需要配置，后续更新指南时可让用户重新查看。
- [x] 私人房间是否计入奖杯需要配置。
- [x] 匹配等级差、奖杯差、等待放宽时间需要配置。

断线、刷新和恢复：

- [x] 刷新大厅后通过 `GET /api/auth/me` 恢复用户。
- [x] 刷新后通过 `GET /api/rooms/current` 恢复当前房间。
- [x] 如果用户正在对局，刷新后通过 `GET /api/matches/current` 恢复当前对局。
- [x] 如果用户正在正式对局，刷新/关闭页面会触发浏览器确认，避免误刷新直接离开当前局。
- [x] 房间 WebSocket 断线后自动重连，并重新拉取 `ROOM_SNAPSHOT`。
- [x] 游戏 WebSocket 断线后自动重连，并请求最新 `GAME_SNAPSHOT`。
- [x] 断线超过配置时间后才判定离开房间或对局。
- [x] 切后台、锁屏、网络切换时前端清空输入，恢复后重新同步状态。
- [x] 用户退出登录或点击游戏内“退出当局”时，如果在对局中调用 `POST /api/matches/current/leave` 按中途退出处理。

接口和类型对齐：

- [x] REST DTO、WebSocket 协议、前端 `types` 必须一致。（完成 100%：已补齐 `MatchSnapshot`、`MatchGameConfig`、房间聊天、房间快照、`GAME_START`、`GAME_SNAPSHOT` 等前端类型）
- [x] 后端字段新增后同步更新 `src/types`。（完成 100%：前端消费到的房间网络质量、当前对局、游戏配置、成员、聊天、通知和匹配字段已同步；数据库内部审计字段不暴露给前端）
- [x] API 文档同步更新 `wildhunt-admin/docs/api/rest-api.md`。
- [x] WebSocket 文档同步更新 `wildhunt-admin/docs/api/websocket-protocol.md`。
- [x] 数据库迁移脚本和本文档保持一致。

日志和排查：

- [x] 后端关键操作写业务日志：登录、创建房间、加入房间、开始游戏、结算、领取奖励。
- [x] 日志不打印 token、密码、API Key。
- [x] 前端关键失败点有可读错误提示。
- [x] 开发环境保留 debug 信息，生产环境默认隐藏。

### 8.1 第一阶段：文档和迁移脚本

- [x] 确认本开发文档。
- [x] 在后端 `docs/db` 新增 V2 迁移说明。
- [x] 在 `wildhunt-web/src/main/resources/db/migration` 新增 `V2__room_match_login_refactor.sql`。
- [x] 添加 `wh_room_chat_message`。
- [x] 添加 `wh_room_kick_log`。
- [x] 调整 `wh_room` 字段。
- [x] 调整 `wh_room_member` 字段。
- [x] 调整 `wh_match_queue` 字段。
- [x] 调整 `wh_user` 字段。
- [x] 调整 `wh_player_profile`，补充奖杯、战绩、指南状态。
- [x] 调整 `wh_match_player`，补充奖杯变化、经验变化和表现统计。
- [x] 调整 `wh_notification`，补充标题和已读时间。
- [x] 新增 `wh_daily_checkin`。
- [x] 新增 `wh_activity`。
- [x] 新增 `wh_activity_claim`。
- [x] 新增 V4 活动条件字段、`wh_season_pass_progress`、`wh_season_pass_claim`。
- [x] 新增 `wh_user_asset`。
- [x] 本地启动后确认迁移成功。（完成 100%：本机 MySQL `wildhunt` 已通过后端 jar 显式 `--spring.flyway.enabled=true --spring.flyway.baseline-on-migrate=true` 实跑；`flyway_schema_history` 显示版本 1 baseline 和版本 2 `room match login refactor` 均成功，且 `wh_room.room_type`、`wh_match_queue.preferred_role`、`wh_room_chat_message` 均已落库可查）

### 8.2 第二阶段：后端房间闭环

- [x] 新增或完善 `RoomMemberMapper`。
- [x] 新增 `RoomChatMessageEntity`、`RoomChatMessageMapper`。
- [x] 新增 `RoomKickLogEntity`、`RoomKickLogMapper`。
- [x] 重构 `RoomService` 使用数据库。（完成 100%：已接入 `RoomMapper`、`RoomMemberMapper`、`RoomKickLogMapper` 的写入/读取；内存 Map 仅作为无数据库开发环境兜底）
- [x] 实现当前房间查询。
- [x] 实现房主退出转移。
- [x] 实现最后一人退出房间关闭。
- [x] 实现房主踢人。
- [x] 实现玩家房间列表只展示 `PLAYER` 房间。
- [x] 补充 `RoomServiceTest`。（完成 100%：已存在房主转移/最后一人关闭、系统房间过滤、开局角色分配专项测试；本轮 `mvn test` 通过）

### 8.3 第三阶段：后端聊天、邀请、WebSocket

- [x] 新增 `RoomChatService`。
- [x] 实现聊天历史接口。
- [x] 实现发送聊天接口。
- [x] 改造 `RoomWebSocketHandler` 按 roomId 分组。
- [x] 成员变化广播 `ROOM_SNAPSHOT`。
- [x] 聊天广播 `ROOM_CHAT_MESSAGE`。
- [x] 踢人广播 `ROOM_KICKED`。
- [x] 邀请好友推送 `ROOM_INVITE_RECEIVED`。（2026-05-24 当前以通知面板/通知接口承载）

### 8.4 第四阶段：后端匹配系统

- [x] 重构 `MatchmakingService` 使用 `wh_match_queue`。（完成 100%：入队、重复保护、匹配成功和取消均接入 `MatchQueueMapper`；无数据库时保留内存等待队列兜底）
- [x] 实现 `WOLF`、`DEER`、`AUTO` 入队。
- [x] 实现重复入队保护。
- [x] 实现取消匹配。
- [x] 实现匹配状态查询。
- [x] 实现系统房间创建但不进入玩家房间列表。
- [x] 实现匹配成功创建对局。（2026-05-24 运行态 `GameMatchService` 已创建对局和玩家快照）
- [x] 实现 AI 鹿补位规则。
- [x] 补充 `MatchmakingServiceTest`。

### 8.5 第五阶段：后端登录

- [x] 新增账号密码注册。
- [x] 新增账号密码登录。
- [x] 新增 `GET /api/auth/me`。
- [x] 密码 BCrypt 哈希。
- [x] 游客用户写入数据库。（完成 100%：`UserService.createGuestUser()` 已写入 `wh_user` 和 `wh_player_profile`，无数据库时保留内存兜底）
- [x] 游客绑定账号预留。
- [x] JWT 过期处理。
- [x] 补充 `AuthServiceTest`。

### 8.6 第六阶段：后端成长、签到、活动、通知、排行榜

- [x] 新增 `CheckinService`。
- [x] 实现每日签到状态查询。
- [x] 实现每日签到领奖。
- [x] 实现连续签到奖励。
- [x] 新增 `ActivityService`。
- [x] 实现活动列表。
- [x] 实现活动进度查询。
- [x] 实现活动奖励领取。
- [x] 新增 `UserAssetService`。
- [x] 实现头像、头像框、称号资产发放。
- [x] 完善 `NotificationService`。
- [x] 实现未读通知数。
- [x] 实现通知列表和已读。
- [x] 完善 `LeaderboardService` 多榜单。
- [x] 对局结算时更新经验、等级、奖杯、战绩、排行榜。（运行态结算已接入 `GameMatchService.settle`）
- [x] 补充 `CheckinServiceTest`。
- [x] 补充 `ActivityServiceTest`。
- [x] 补充 `LeaderboardServiceTest`。

### 8.7 第七阶段：前端登录页

- [x] 新增 `src/auth/login-page.ts`。
- [x] 新增登录页样式。
- [x] 使用 `src/assets/logo/bj.png` 作为背景。
- [x] 参考 `参考登录图.png` 调整布局、颜色、按钮。
- [x] 实现游客进入。
- [x] 实现登录。
- [x] 实现注册。
- [x] 实现 token 恢复。
- [x] 登录成功进入大厅。

### 8.8 第八阶段：前端成长、签到、活动、通知、排行榜

- [x] 玩家卡片展示头像、昵称、等级、经验条、奖杯。
- [x] 每日签到按钮打开签到弹窗。
- [x] 签到弹窗展示 7 日奖励。
- [x] 签到成功刷新经验和等级。
- [x] 活动中心按钮打开活动列表。
- [x] 活动列表展示奖励、条件、领取状态。
- [x] 活动奖励可领取。
- [x] 活动头像、头像框、称号进入用户资产。
- [x] 新手指南按钮打开玩法指导。
- [x] 新用户首次进入自动弹新手指南。
- [x] 右上角通知按钮显示未读数量。
- [x] 通知面板支持系统通知、好友申请、房间邀请、活动奖励、对局结果。
- [x] 排行榜支持奖杯总榜、狼王榜、鹿王榜、速胜榜、生存榜、好友榜。
- [x] 对局结束后展示经验变化和奖杯变化。

### 8.9 第九阶段：前端房间状态和 tab

- [x] 新增 `currentView` 状态。
- [x] 点击大厅不退出房间。
- [x] 点击房间但未加入时提示 `请先加入房间`。
- [x] 在房间 tab 下方显示当前房间号标识。
- [x] 刷新后恢复当前房间。
- [x] “返回大厅”改为“退出房间”。
- [x] 退出房间调用后端接口。
- [x] 被踢后清空房间状态。

### 8.10 第十阶段：前端房间页 UI

- [x] 移除右侧游戏设置卡片。
- [x] 房间信息卡片收紧。
- [x] 聊天区卡片收紧并支持滚动。
- [x] 中间玩家卡片动态渲染 `members`。
- [x] 使用好友卡片背景图风格。
- [x] 空位卡片支持邀请好友。
- [x] 房主可踢人。
- [x] 右键好友出现邀请菜单。
- [x] 房间内聊天输入、发送、历史显示。

### 8.11 第十一阶段：前端匹配和房间列表

- [x] 新增 `src/api/matchmaking-api.ts`。
- [x] 狼匹配调用后端队列。
- [x] 鹿匹配调用后端队列。
- [x] 右下角开始游戏调用 `AUTO` 队列。
- [x] 匹配中显示状态和取消按钮。
- [x] 匹配成功进入游戏。
- [x] 房间列表只显示玩家自建房间。
- [x] 移除本地 sample room 对正式列表的影响。

### 8.12 第十二阶段：前端游戏正式对局迁移

- [x] 拆分 `src/game/game-scene.ts`。（完成 100%：已有 `game-actions.ts`、`game-actors.ts`、`game-config.ts`、`game-loader.ts`、`game-audio.ts`、`game-runtime-config.ts`；本轮继续拆出 `game-scene-types.ts`、`game-math.ts`、`game-collision.ts`、`game-terrain-sampler.ts`、`game-scent.ts`、`game-nature-batcher.ts`、`game-input-controller.ts`，并把正式 WS 输入同步收进 `game-network.ts`，主场景由 2497 行降到 2019 行）
- [x] 新增 `mountGameScene(options)` 正式入口。
- [x] 移除正式流程对 `localStorage.wildhunt.assignedRole` 的依赖。
- [x] 正式流程禁用本地选阵营弹窗。
- [x] `DEV_LOCAL_MODE` 默认只在开发环境开启。
- [x] 将 `loader.load` 改为 Promise 并纳入加载界面。
- [x] 加载地形、自然物、狼模型、鹿模型、音频、HUD、WebSocket 首帧后才倒计时。（完成 100%：`game-loader.ts` 的加载闸门已在正式初始化流程中使用，地形、自然物、狼/鹿模型、HUD、`game-audio.ts` 程序化音频和正式对局 `GAME_SNAPSHOT` 首帧全部完成后才 `resetRound()`；正式 WS 首帧超时会停在加载错误，不再用固定时间兜底开局）
- [x] 使用后端 `matchSeed` 生成一致地图、食物点和 AI 鹿初始状态。（完成 100%：正式入口传入 `matchSeed`，游戏初始化前设置确定性随机源，地形、食物点、自然物和 AI 鹿初始分布使用同一随机序列）
- [x] 使用后端 `gameConfig` 替代硬编码 `REAL_DEER_COUNT`、`AI_DEER_COUNT`、时间、技能冷却。
- [x] 每 20Hz 发送 `PLAYER_INPUT`。
- [x] 消费 `GAME_SNAPSHOT` 并插值渲染远端玩家。（当前已消费服务端快照，远端插值仍是轻量 MVP）
- [x] HUD 倒计时以后端快照为准。
- [x] 狼气味、扑咬、鹿环顾、进食、伪装改为服务端确认。（完成 100%：`GameWebSocketHandler` 将 `PLAYER_INPUT` 交给 `GameMatchService.applyInput()`，后端确认 `WOLF_SCENT`、`WOLF_POUNCE`、`DEER_LOOK`、`DEER_EAT`、`DEER_CAMOUFLAGE` 并随 `GAME_SNAPSHOT.skillConfirm` 广播；命中、死亡、超时和 AI 狼追击进入服务端运行态）
- [x] 正式结算使用 `MATCH_END`。
- [x] 结算弹窗显示经验、等级、奖杯、排行榜变化。
- [x] 结算按钮包含返回房间、再匹配、返回大厅。（当前正式对局结算按钮改为返回大厅；游戏 HUD 常驻“退出当局”，后续再补返回房间/再匹配的视觉细分）
- [x] debug 文本和碰撞调试只在开发模式显示。

### 8.13 第十三阶段：手机横屏适配

- [x] 新增移动端检测和横屏判断。
- [x] 竖屏显示旋转手机提示。
- [x] 登录页适配手机横屏。
- [x] 大厅页适配手机横屏。
- [x] 房间页适配手机横屏。
- [x] 房间聊天在手机横屏以抽屉打开。
- [x] 好友右键菜单在手机改成长按或更多按钮。
- [x] 游戏 HUD 适配手机横屏。
- [x] 新增虚拟摇杆。
- [x] 新增移动端技能按钮和冷却遮罩。
- [x] 移动端默认应用省电/均衡画质。
- [x] 移动端禁用或降低 debug、SSAO、阴影、粒子、植被密度。
- [x] 验证 `667 x 375`、`844 x 390`、`932 x 430`、`915 x 412`、`1024 x 768`。

### 8.14 第十四阶段：全链路保障和文档对齐

- [x] 梳理 REST 权限矩阵。
- [x] 梳理 WebSocket 权限矩阵。
- [x] 为房间、匹配、结算、签到、活动领取补事务。
- [x] 为创建房间、加入房间、开始游戏、匹配、签到、活动领取补幂等。
- [x] 为登录、聊天、好友申请、邀请、匹配补限流。（完成 100%：`AuthService`、`RoomChatService`、`FriendService`、`InviteService`、`MatchmakingService` 均已接入基础限流）
- [x] 为昵称、房间名、聊天内容补敏感词过滤。（完成 100%：昵称注册/资料更新、房间聊天和房间名均走 `ContentGuard.cleanDisplayText()`；本轮 REST 冒烟验证含敏感词房间名会被替换为 `***`）
- [x] 为断线、刷新、重连、退出登录补恢复逻辑。（完成 100%：登录、当前房间和当前对局刷新恢复已接入；房间/大厅/游戏 WS 使用自动重连；退出登录前会先退出房间并清理连接/对局参数）
- [x] 新增 `GET /api/matches/current`。
- [x] 同步更新前端 `src/types`。
- [x] 同步更新后端 REST API 文档。
- [x] 同步更新 WebSocket 协议文档。
- [x] 准备默认配置、默认活动、默认签到、默认资产、默认赛季数据。
- [x] 检查日志不打印 token、密码、API Key。

### 8.15 第十五阶段：联调和验收

- [x] 后端 `mvn test` 通过。
- [x] 后端 `mvn package` 通过。
- [x] 前端 `npm run build` 通过。
- [x] 浏览器 A 创建房间。
- [x] 浏览器 B 加入房间。
- [x] A 切大厅，仍显示房间驻留标识。
- [x] A 点击房间 tab 能回房间。
- [x] 未入房用户点击房间 tab 提示请先加入房间。
- [x] 房主 A 退出后，B 成为房主。
- [x] 房间只剩 A 时退出，房间关闭且列表消失。
- [x] 房主能踢人。
- [x] 房间内聊天实时显示。
- [x] 狼匹配和鹿匹配能组成系统对局。
- [x] 右下角开始游戏能作为 AUTO 补位。
- [x] 系统房间不显示在玩家房间列表。
- [x] 登录页刷新和 token 恢复正常。
- [x] 正式对局不能通过 localStorage 改角色。
- [x] 正式对局不显示本地选阵营弹窗。
- [x] 模型和地形加载完成前不会开局。
- [x] 所有客户端使用同一个 matchSeed，AI 鹿和食物点关键布局一致。
- [x] 倒计时结束前玩家不能移动或释放技能。
- [x] 远端玩家位置通过服务端快照同步。
- [x] 狼扑咬、气味、鹿环顾、进食、伪装以后端判定为准。（完成 100%：与 8.11 服务端确认项交叉完成，正式 WS 输入由后端状态机判定，快照只广播同一 `matchId` 会话）
- [x] 对局结果来自 `MATCH_END`，不是客户端本地 `finishRound()` 私自结算。（完成 100%：后端 `GameWebSocketHandler` 在服务端状态机结束时广播 `MATCH_END`；前端正式对局中 `finishRound()` 对非服务端确认结果直接返回，本地碰撞/超时不再私自结算）
- [x] 手机横屏大厅、房间、游戏均可正常操作。
- [x] 手机竖屏显示旋转提示。
- [x] 手机横屏虚拟摇杆和技能按钮可多点触控。
- [x] 手机横屏没有关键 UI 被刘海、安全区或系统手势遮挡。
- [x] 手机横屏游戏帧率达到可玩标准。
- [x] 重复点击创建房间、开始游戏、签到、活动领取不会产生重复数据。
- [x] 刷新页面后可恢复登录、当前房间、当前对局。
- [x] 断线重连后房间和游戏状态能同步回来。
- [x] 未授权用户不能访问房间、聊天、对局、奖励接口。
- [x] 聊天、好友申请、邀请、匹配有基础限流。
- [x] REST 文档、WebSocket 文档、数据库迁移、前端类型保持一致。
- [x] 每日签到可领取经验并刷新等级。
- [x] 活动中心可领取经验、头像、头像框或称号。
- [x] 新手指南能打开玩法说明。
- [x] 右上角通知能收到系统通知、房间邀请、对局结果。
- [x] 排行榜按奖杯和不同榜单正确展示。
- [x] 1 级新手不会直接匹配到 100 级老玩家。
- [x] 对局结算能产出经验、奖杯、战绩和排行榜变化。

---

## 9. 风险和处理

- [x] 当前后端核心服务仍是内存实现，落库重构会影响范围较大；建议先写 V2 迁移，再逐步替换服务。
- [x] 前端 `lobby-page.ts` 当前承担过多职责，改造时建议拆出 `room-scene.ts`、`room-chat.ts`、`room-tabs.ts`，避免继续堆大文件。
- [x] 当前部分中文显示在文件输出中存在乱码痕迹，开发时需要确认源文件实际编码为 UTF-8。
- [x] WebSocket 当前未做鉴权和房间隔离，必须在房间聊天和踢人开发前先修。（2026-05-24 已补房间 WebSocket token 校验和 roomId 分组）
- [x] 匹配服务的“鹿匹配没有真人狼时怎么办”已按当前产品要求确定：不暴露真人缺口，鹿单人匹配直接启用服务端 AI 狼兜底。
- [x] 登录页图片较大，需验证首屏加载速度，必要时压缩背景图。

---

## 10. 建议提交顺序

- [x] `docs: add room matchmaking login refactor plan`。（完成 100%：作为建议提交拆分项，文档内容已覆盖；当前目录非 git 仓库，不执行真实提交）
- [x] `db: add room chat kick and queue migration`。（完成 100%：作为建议提交拆分项，迁移内容已覆盖；当前目录非 git 仓库，不执行真实提交）
- [x] `backend: persist room membership lifecycle`
- [x] `backend: add room chat and owner controls`。（完成 100%：聊天 HTTP/WS、历史落库、系统消息、房主踢人、房主转移、`ROOM_KICKED`/`ROOM_SNAPSHOT` 推送均已接入）
- [x] `backend: implement matchmaking queues`。（完成 100%：`WOLF`/`DEER`/`AUTO` 队列、重复入队保护、取消、匹配成功、`wh_match_queue` 状态写入和系统对局创建均已接入）
- [x] `backend: add account login and session restore`。（完成 100%：游客、注册、登录、`/auth/me`、游客绑定、JWT、用户/资料落库和登录限流均已接入）
- [x] `frontend: add login page`。（完成 100%：作为建议提交拆分项，前端登录页内容已完成；当前目录非 git 仓库，不执行真实提交）
- [x] `frontend: refactor lobby room tab state`。（完成 100%：作为建议提交拆分项，房间 tab 状态改造已完成；当前目录非 git 仓库，不执行真实提交）
- [x] `frontend: rebuild room scene chat and member cards`。（完成 100%：动态成员卡、聊天历史/发送、被踢推送、好友右键/更多邀请和房主踢人均已接入）
- [x] `frontend: wire matchmaking and player room list`。（完成 100%：玩家房间列表、狼/鹿/AUTO 匹配、取消状态、按钮禁用、match options 会话存储和正式入口均已接入）
- [x] `test: verify room matchmaking login flows`。（完成 100%：本轮使用 JBR 17 + IntelliJ Maven 通过后端 `mvn.cmd test`、`mvn.cmd package`，通过前端 `npm.cmd run build`；临时 `8082` 后端 REST 冒烟验证游客登录、`/auth/me`、鹿匹配 AI 补位、`/matches/current`、房间名过滤、好友 `IN_ROOM` 状态和 `/api/admin/cleanup/all`，验证后已停止临时进程）




